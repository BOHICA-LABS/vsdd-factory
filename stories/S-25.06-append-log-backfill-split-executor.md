---
document_type: story
level: implementation
story_id: "S-25.06"
epic_id: "E-25"
version: "1.2"
last_amended: "2026-09-25 (v1.2) — Spec-First Gate S-7.01 CLOSED; status draft -> ready (story-writer, same burst as BC-1.18.013 v1.1/VP-143-145 authorship). Gate 1 (BC authorship) SATISFIED: product-owner authored NEW BC-1.18.013 (\"Governed One-Time Migration for the Mechanism-A Backfill-Split of the Four Append-Log Files, Activation via the ADR-052 Sanctioned Execution Path\"); behavioral_contracts: [] -> [BC-1.18.013]. Gate 2 (VP allocation) SATISFIED: architect allocated VP-143 (integration; four-file all-or-nothing atomicity + two-layer idempotency), VP-144 (proptest; per-file delegation-correctness), VP-145 (integration/safety; closed-grammar rejection); verification_properties: [] -> [VP-143, VP-144, VP-145]. Activation Gate / Architecture Compliance Rule 7 OPEN contradiction (FINDING HIGH-2) RESOLVED per ADR-052 §Decision 9 (ratified 2026-09-20, D-1232, with S-25.06 as an explicit input): the invocation is a `Bash`-tool execution of the native `factory-dispatcher backfill-append-logs` binary under ADR-052's closed argument grammar + armed-activation manifest, NOT an Edit/Write tool call — Rule 7 text replaced verbatim with ADR-052 §Decision 9's replacement wording. Superseded provisional AC-001–AC-008 with 14 BC-1.18.013-anchored ACs (AC-001..AC-014); CRITICAL FIX: AC-001(b)'s auto-discovery-by-cycle-directory language was WRONG — ADR-052 §Decision 3's ratified closed grammar takes ZERO path/cycle arguments; the four target files are the FIXED set named in ADR-052 §Decision 8's allowlist (decision-log.md, burst-log.md, lessons.md, session-checkpoints.md under v1.0-brownfield-backfill/), not auto-discovered. T-1's architect activation decision (which binary hosts the subcommand) is RESOLVED by ADR-052 §Decision 2: `crates/factory-dispatcher/` (co-located with `shard_manager.rs`), NOT `last-amended-migrate` — target_module, Architecture Mapping, Library & Framework Requirements, and File Structure Requirements sections updated accordingly (sibling-sweep, TD-VSDD-060 class). Token Budget updated to add BC-1.18.013, BC-1.18.011 (structural template), VP-143/144/145, and ADR-052 read-at-activation rows. Noted BC-1.18.013 Postcondition 8's operational precondition (`.factory/shard-config.toml` needs an `artifact-path-registry.yaml` entry, pre-existing ADR-053 gap, devops-engineer/architect scope) as a delivery-time prerequisite for the ShardRegistry-enrollment AC (AC-010) — flagged for implementer visibility, does not block this story's own dispatch-readiness per BC-1.18.013 Postcondition 8's own carve-out. depends_on: [] / blocks: [S-25.02] confirmed unchanged (product-owner: no change needed). input-hash recompute owed to state-manager (`compute-input-hash S-25.06-append-log-backfill-split-executor.md --update`). --- (v1.1 detail retained:) 2026-09-12 (v1.1) — Adversarial direction review remediation (story-writer; human-approved session). FINDING HIGH-1: broke circular depends_on/blocks cycle — depends_on: [S-25.02] → depends_on: [] (mechanism run_mechanism_a_backfill_split was delivered in S-25.02 cluster-3 PR #831 MERGED; no unmet story prerequisite); blocks: [] → blocks: [S-25.02] (S-25.02 Cohort-B flip cluster-7 must not dispatch until this backfill has run; acyclic representation per Sequencing note in body). FINDING MED-3: wave: null → wave: W3 (follows S-25.02 W2; mechanism already delivered); risk_mitigations populated (R-S25.06-001/002/003). FINDING HIGH-2: Rule 7 in Architecture Compliance Rules annotated with OPEN activation gate — impossible execution-path contradiction noted; architect resolution required before status: ready. FLAG for state-manager: S-25.02 body should note that cluster-7 Cohort-B flip is blocked on S-25.06 completion (see FLAG block in body §Dependency Sequencing). --- (v1.0 detail retained:) 2026-09-12 (v1.0) — Initial authoring (story-writer). Mechanistic-coverage gap discovered this session: run_mechanism_a_backfill_split exists in shard_manager.rs (shipped in S-25.02 cluster-3, PR #831) but has no wired CLI entry point for the four cycle append-log files, and the append-log artifact class is not enrolled in the ShardRegistry for ongoing cap-triggered rotation. This story captures the correct fix under a separate story per single-writer and Canonical Principle disciplines. Status: draft pending product-owner BC authorship (Spec-First Gate S-7.01). Refs: BC-1.18.008 (prior-art), BC-1.18.005 (prior-art), BC-1.18.011 (prior-art), BC-10.13.001 (prior-art), S-25.02 cluster-3 (PR #831)."
status: ready
producer: story-writer
timestamp: 2026-09-12T00:00:00Z
phase: 3
inputs:
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.013.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.008.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.005.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.006.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.011.md
  - .factory/specs/behavioral-contracts/ss-10/BC-10.13.001.md
  - .factory/specs/verification-properties/VP-143.md
  - .factory/specs/verification-properties/VP-144.md
  - .factory/specs/verification-properties/VP-145.md
  - .factory/specs/architecture/decisions/ADR-051-layer-2-two-mechanism-size-triggered-shard-rotation-append-logs-and-bc-index-sharding.md
  - .factory/specs/architecture/decisions/ADR-052-native-migration-cli-bash-tool-allowlist-sanctioned-execution-path.md
  - .factory/specs/prd-supplements/error-taxonomy.md
  - .factory/stories/S-25.02-artifact-sharding-layer2.md
  - crates/factory-dispatcher/src/shard_manager.rs
input-hash: "e238061"
# input-hash STALE as of v1.2 (frontmatter + body rewritten this burst) — recompute owed to
# state-manager: `compute-input-hash .factory/stories/S-25.06-append-log-backfill-split-executor.md --update`
# (POLICY 18).
traces_to: .factory/stories/epics/E-25-validation-integrity.md
points: "8"
depends_on: []
# FINDING HIGH-1 FIX (2026-09-12): depends_on set to [] because the prerequisite
# mechanism (run_mechanism_a_backfill_split) was DELIVERED in S-25.02 cluster-3,
# PR #831 MERGED. There is no unmet story-level prerequisite.
# The prior depends_on: [S-25.02] created a deadlock cycle: S-25.02's Cohort-B
# flip capstone (cluster-7) cannot dispatch until this backfill has run, yet
# S-25.06 was blocking on S-25.02 completing — a circular dependency.
# The acyclic representation: mechanism delivered (S-25.02 cluster-3 DONE) →
# this story wires + executes the mechanism → S-25.02 cluster-7 flip activates.
blocks: [S-25.02]
# blocks: [S-25.02] encodes the correct sequencing: S-25.02's Cohort-B fail-closed
# flip (cluster-7 capstone) MUST NOT be dispatched until this story (S-25.06) has
# successfully executed the backfill against the live cycle files (Task T-10).
# FLAG for state-manager: S-25.02 should be annotated in its body to note that
# cluster-7 is blocked on S-25.06 completion. Do NOT edit S-25.02's body here —
# flag this for state-manager dispatch to add a reciprocal note to S-25.02.
behavioral_contracts: [BC-1.18.013]
# BC status: Spec-First Gate S-7.01 CLOSED (2026-09-25). Product-owner authored BC-1.18.013 v1.1
# ("Governed One-Time Migration for the Mechanism-A Backfill-Split of the Four Append-Log Files,
# Activation via the ADR-052 Sanctioned Execution Path") specifying the sanctioned CLI executor
# contract (`backfill-append-logs`), the closed argument grammar, the ShardRegistry enrollment
# step (Postcondition 8), and the one-time activation-step invocation sequence. See §Behavioral
# Contracts table below for this BC plus the lineage/prior-art BCs it invokes.
verification_properties: [VP-143, VP-144, VP-145]
# VP status: allocated by architect (2026-09-25, S-25.06 Spec-First Gate closure, VP-INDEX v3.24).
# VP-143 (integration): four-file all-or-nothing atomicity + two-layer idempotency consistency.
# VP-144 (proptest): per-file delegation-correctness against BC-1.18.008 Postcondition 6(a)/(b).
# VP-145 (integration/safety): closed-grammar rejection invariant for `backfill-append-logs`.
priority: "P1"
cycle: v1.0-brownfield-backfill
wave: "W3"
# W3: S-25.06 follows S-25.02 (wave "W2") which delivered the mechanism. The
# mechanism is already merged; this story wires and executes it. W3 is appropriate.
target_module: crates/factory-dispatcher
# RESOLVED 2026-09-25 (was crates/last-amended-migrate — T-1 placeholder): ADR-052 §Decision 2
# ratifies binary placement in crates/factory-dispatcher/, co-located with shard_manager.rs.
# last-amended-migrate is NOT the host binary for this subcommand (BC-10.13.001's 5-file scope
# is unaffected/unexpanded).
subsystems: [SS-01]
estimated_days: 4
assumption_validations: []
risk_mitigations:
  - R-S25.06-001
  - R-S25.06-002
  - R-S25.06-003
# R-S25.06-001: Append-logs keep growing every burst through clusters 5-7 while
# validation is fail-open (validate-burst-log PostToolUse on_error=continue → validation
# silently LOST on oversized files). Mitigation: this story is P1; backfill-split
# execution must complete before W3 gate closes to bound further growth. Each burst
# dispatched without the backfill having run adds unvalidated content to already-oversized
# files, deepening the fuel-exhaustion exposure.
# R-S25.06-002: DEFAULT_FUEL_CAP relief (20M on develop) is NOT operative at operator
# level (rc.23 embeds 10M). CLAUDE.md: "20M does not eliminate exhaustion for large cycle
# artifacts." Growth risk remains live through all clusters 5-7 and beyond. Mitigation:
# ShardRegistry enrollment (AC-010, renumbered 2026-09-25) activates BC-1.18.005 cap-triggered
# rotation after the backfill runs, preventing re-inflation. Until enrollment fires, every future
# write to burst-log.md or decision-log.md bypasses the cap check.
# R-S25.06-003: If mutation tooling (cargo mutants / mutagen) cannot handle further growth
# of shard_manager.rs or the append-log cluster files before this story ships, mutation
# testing falls back to manual boundary-value analysis — a quality regression. Stop
# condition: if file sizes reach a point where the standard mutation tool invocation
# exceeds time bounds in CI, the Red Gate + 3-CLEAN convergence gate for this story
# blocks PR readiness, preventing dispatch of the capstone cluster-7 flip. The stop
# condition is the existing PR gate — no separate escape valve is needed.
tdd_mode: strict
---

> **READY — Spec-First Gate S-7.01 CLOSED (2026-09-25).** Both pre-dispatch gates have cleared:
>
> 1. **BC authorship — SATISFIED.** Product-owner authored `BC-1.18.013` v1.1 ("Governed
>    One-Time Migration for the Mechanism-A Backfill-Split of the Four Append-Log Files,
>    Activation via the ADR-052 Sanctioned Execution Path"), specifying the sanctioned CLI
>    executor contract (`backfill-append-logs`), ShardRegistry enrollment for the four append-log
>    files (Postcondition 8), and the one-time activation-step invocation sequence. Referenced in
>    `behavioral_contracts: [BC-1.18.013]`.
> 2. **VP allocation — SATISFIED.** Architect allocated `VP-143` (integration; four-file
>    all-or-nothing atomicity + two-layer idempotency consistency), `VP-144` (proptest; per-file
>    delegation-correctness against BC-1.18.008 Postcondition 6(a)/(b)), and `VP-145`
>    (integration/safety; closed-grammar rejection invariant). Referenced in
>    `verification_properties: [VP-143, VP-144, VP-145]`.
>
> This story is registered under E-25 per CLAUDE.md Canonical Principle §2 (feature ordering is
> the only acceptable speed lever). It moved from backlog to `status: ready` in this burst; the
> Activation Gate (FINDING HIGH-2, Architecture Compliance Rule 7) is likewise RESOLVED — see
> below.

> **tdd_mode: strict** — Full TDD Iron Law enforced: all non-trivial function bodies use
> `todo!()`; Red Gate density check >= 0.5 required before Step 4 dispatch.

> **Dependency Sequencing (FINDING HIGH-1 fix — 2026-09-12):**
> The prior `depends_on: [S-25.02]` created a cycle: S-25.02's Cohort-B fail-closed flip
> (cluster-7 capstone) cannot dispatch until this backfill has run, yet S-25.06 was
> blocking on S-25.02 completing — deadlock.
>
> **Acyclic representation adopted:**
> - `depends_on: []` — The prerequisite mechanism (`run_mechanism_a_backfill_split`) was
>   delivered in S-25.02 cluster-3 (PR #831 MERGED). No unmet story-level prerequisite.
> - `blocks: [S-25.02]` — S-25.02's Cohort-B flip (cluster-7) is blocked on S-25.06
>   completing Task T-10 (live backfill execution). The sequencing is:
>   S-25.02 cluster-3 delivers mechanism (DONE) → S-25.06 wires + executes mechanism
>   → S-25.02 cluster-7 flip activates. This is strictly acyclic.
>
> **FLAG for state-manager (do NOT edit S-25.02 in this burst):**
> S-25.02's body should receive a reciprocal note in its cluster-7 task block stating:
> "Cluster-7 (Cohort-B fail-closed flip) is blocked on S-25.06 completion. S-25.06
> must execute the live backfill (Task T-10) and have its PR merged before cluster-7
> can be dispatched. See `blocks: [S-25.02]` in S-25.06 frontmatter and this sequencing
> note." State-manager should add this note to S-25.02 in the same burst that registers
> S-25.06 in STORY-INDEX (or in a follow-up S-25.02 amendment burst).

> **Activation Gate — RESOLVED (FINDING HIGH-2 closed per ADR-052 §Decision 9, 2026-09-25):**
> See Architecture Compliance Rule 7 below. The architect resolved the invocation-mechanism
> contradiction: the activation step is a `Bash`-tool execution of the native
> `factory-dispatcher backfill-append-logs` binary at its absolute trusted path, under ADR-052's
> closed argument grammar (§Decision 3) and armed-activation manifest (§Decision 4) — NOT an
> Edit/Write tool call. ADR-052 was authored with S-25.06 explicitly as an input and was
> human-ratified 2026-09-20 (D-1232). No further architect action is required for this gate.

# S-25.06: Append-Log Artifact Class — Sanctioned Backfill-Split Executor + ShardRegistry Enrollment + Cap-Triggered Rotation

## Narrative

- **As a** factory pipeline operator who depends on the four cycle append-log files
  (`decision-log.md`, `burst-log.md`, `lessons.md`, `session-checkpoints.md`) being compactable
  without requiring a manual shell write that the Claude Code auto-mode permission classifier
  correctly blocks as a POL-3 / TD-FACTORY-HOOK-BYPASS-001 P0 bypass
- **I want** a sanctioned, hook-validated, NATIVE (non-WASM, fuel-safe) CLI executor that:
  (a) performs the BC-1.18.008-specced one-time backfill-split against each of the four
  currently-oversized files, wiring the already-implemented `run_mechanism_a_backfill_split`
  function to an actual invocation path; and
  (b) enrolls those files in the ShardRegistry so ongoing cap-triggered rotation fires
  automatically per the BC-1.18.005 size-trigger mechanism — eliminating the class of manual
  compaction operations that have no sanctioned executor today
- **So that** no agent or human operator ever needs a bare `cp`/shell write to compact a cycle
  append-log file again, the WASM fuel-budget exhaustion and hook-block anti-patterns documented
  in CLAUDE.md §WASM plugin fuel budgets are permanently eliminated for the append-log class,
  and the BC-1.18.008 mandatory one-time backfill mandate is actually discharged rather than
  remaining a spec obligation without an execution path

## Context / Feature Summary

**Origin:** This story captures a mechanistic-coverage gap discovered at S-25.02 cluster-3
post-merge review. BC-1.18.008 specced "a mandatory, one-time backfill-split operation, executed
once at F4 activation" for `decision-log.md`, `burst-log.md`, `lessons.md`, and
`session-checkpoints.md`. S-25.02 cluster-3 (PR #831) shipped the ALGORITHM — specifically
`run_mechanism_a_backfill_split` in `crates/factory-dispatcher/src/shard_manager.rs` —
implementing all postconditions, idempotency, crash-atomicity, Backfill Recovery Manifest, and
independent census. But this function has exactly ZERO production call sites. The four files are
NOT enrolled in any ShardRegistry config; no CLI subcommand invokes the function for them.

### The Two Layers of the Gap

**Layer 1 — One-time backfill execution (BC-1.18.008 obligation unpaid):**
`run_mechanism_a_backfill_split` requires: (a) a `ShardEntry` with the artifact's metadata, (b)
the canonical file path, (c) `record_boundary_offsets` computed from the BC-1.18.008 marker
regexes for that artifact, and (d) `retention_count`. Today there is no code path that supplies
these four arguments for the real live files. The function can be called in unit tests with
synthetic fixtures; it cannot be called against `/repo/.factory/cycles/*/decision-log.md` without
a new CLI entry point.

As of 2026-09-12, the files are materially oversized:
- `v1.0-brownfield-backfill/decision-log.md`: ~1,233 KB / ~10,445 lines
- `v1.0-brownfield-backfill/session-checkpoints.md`: ~1,228 KB
- `v1.0-brownfield-backfill/burst-log.md`: ~953 KB
- `v1.0-brownfield-backfill/lessons.md`: ~291 KB
- `v1.0-feature-engine-discipline-pass-1/decision-log.md`: ~251 KB / ~564 lines
- `v1.0-feature-engine-discipline-pass-1/burst-log.md`: ~629 KB
- `v1.0-feature-engine-discipline-pass-1/lessons.md`: ~155 KB
Every one of these is at or beyond the WASM-fuel-safe budget documented in ADR-042 / ADR-051.

**Layer 2 — No ongoing cap-triggered rotation (BC-1.18.005/BC-1.18.006 not wired for this class):**
Even after the one-time backfill runs, the four files will continue to grow. The BC-1.18.005
size-trigger + BC-1.18.006 roll mechanism fires only for artifacts enrolled in the ShardRegistry.
Without enrollment, every future write to `burst-log.md` or `decision-log.md` bypasses the
cap check, and the files will re-inflate without any automatic compaction.

### The Constraint: No Manual Shell Writes

The correct fix is NOT a human-authorized shell `cp` or a one-off agent `python -c`. Those paths
are the exact anti-patterns POL-3 / TD-FACTORY-HOOK-BYPASS-001 P0 exist to prevent. **RESOLVED
2026-09-25 per ADR-052 §Decision 9** (see Architecture Compliance Rule 7 below): the
production-grade fix is NOT an Edit/Write-tool-mediated write either — it is a sanctioned,
one-time, human-approved `Bash`-tool execution of a native `factory-dispatcher
backfill-append-logs` binary under a closed argument grammar (ADR-052 §Decision 3), an
armed-activation manifest (§Decision 4), and a native admission gate (§Decision 5a) — so the
compaction is auditable (stdout census report + durable txn record, §Decision 6/7), crash-safe,
and structurally distinct from an unvalidated `cp`/`python -c` bypass without requiring the
Edit/Write tool surface to mediate the four files' content directly (which BC-1.18.013's
Postconditions establish is unnecessary: the binary's own admission gate and armed-activation
manifest are the sanctioning mechanism, not the Edit/Write tool call itself).

### What the `last-amended-migrate` CLI Cannot Do

`last-amended-migrate`'s `TARGET_FILES` const is hard-coded to 5 files (STATE.md, BC-INDEX.md,
STORY-INDEX.md, ARCH-INDEX.md, VP-INDEX.md). It handles frontmatter `changelog:`/`last_amended`
fields only (BC-10.13.001 scope). It cannot apply the content-body record-boundary split required
by BC-1.18.008. **RESOLVED 2026-09-25 per ADR-052 §Decision 2:** the missing entry point is a NEW
`backfill-append-logs` subcommand added to the `factory-dispatcher` binary (co-located with
`shard_manager.rs`), NOT an extension of `last-amended-migrate`. BC-10.13.001's 5-file scope is
unaffected.

## Acceptance Criteria

> Superseded 2026-09-25: the provisional AC-001–AC-008 (product-owner-reference placeholders
> pending BC authorship) are REPLACED by the 14 BC-1.18.013-anchored ACs below, now that
> BC-1.18.013 v1.1 is authored and this story's Spec-First Gate is closed. Every AC traces to a
> BC-1.18.013 precondition/postcondition/invariant/edge-case clause. **CRITICAL FIX:** the old
> AC-001(b) ("accepts... a cycle directory and auto-discovers the four canonical files within it")
> is WRONG and is not carried forward — ADR-052 §Decision 3's ratified closed argument grammar
> takes ZERO path/cycle arguments; AC-001 below reflects the ratified closed-grammar reality.

### AC-001 (traces to BC-1.18.013 Precondition 3, Postcondition 6, Invariant 5, EC-008)
The `backfill-append-logs` subcommand of the `factory-dispatcher` binary accepts EXACTLY two
invocation forms — `backfill-append-logs` and `backfill-append-logs --census` — and no others.
It takes NO path argument, NO cycle-directory argument, and NO auto-discovery mode. The four
target files are the FIXED set named in ADR-052 §Decision 8's allowlist:
`.factory/cycles/v1.0-brownfield-backfill/{decision-log.md,burst-log.md,lessons.md,session-checkpoints.md}`.
Any invocation outside the two accepted forms (a path argument, a `--cycle` flag, an extra flag,
a shell metacharacter, or a compound command) is REJECTED by the pre-shell classifier
(ADR-052 §Decision 5c) before the binary is even invoked, and rejected again by the binary itself
with a non-zero exit if somehow bypassed.

### AC-002 (traces to BC-1.18.013 Postcondition 1)
For EACH of the four target files independently, content-preservation is verified via
BC-1.18.008 Postcondition 6(a)'s existing byte-for-byte record-boundary-safe partition check
(concatenation of all resulting shards plus the final current file reproduces the original
monolithic file byte-for-byte). This BC introduces no new per-file content-preservation
mechanism; the check is performed at ADR-052 §Decision 7c step 3b, per file, before the
CURRENT.json pointer swap.

### AC-003 (traces to BC-1.18.013 Postcondition 2)
For EACH of the four target files independently, the independent census is verified via
BC-1.18.008 Postcondition 6(b)'s existing record-integrity check (every structural record present
in the original file appears in EXACTLY ONE resulting shard). There is no cross-file ID oracle;
each file's own record set is closed and self-contained. Verified at ADR-052 §Decision 7c step
3b, per file, before the pointer swap.

### AC-004 (traces to BC-1.18.013 Postcondition 3, Invariant 3)
The migration's atomic unit is the four SEPARATE files sharing ONE txn record, ONE generation,
and ONE `CURRENT.json` pointer swap. If content-preservation (AC-002) or census (AC-003) fails
for ANY ONE of the four files, the ENTIRE migration aborts: none of the four canonical files is
touched, not merely the failing one. Each atomic file replacement (`rename(2)`), once past the
pointer swap, is followed by the platform-appropriate durability barrier (Linux:
`fsync(file_fd)` + `fsync(parent_dir_fd)`; macOS/APFS: `fcntl(F_FULLFSYNC)` + best-effort
`fsync(dir_fd)`) per ADR-052 §Decision 7d.

### AC-005 (traces to BC-1.18.013 Postcondition 3a)
Immediately before the `CURRENT.json` pointer swap (and exactly once, never per-file), the
migration binary re-reads all four target files' current content, computes a single SHA-256 over
their concatenated bytes in fixed order (`decision-log.md`, `burst-log.md`, `lessons.md`,
`session-checkpoints.md`), and compares it against `source_sha256` in the txn record. Any
divergence aborts with `FINGERPRINT_MISMATCH_ABORT` (exit 2); all four canonical files are left
untouched; re-activation is required.

### AC-006 (traces to BC-1.18.013 Postcondition 4, EC-001)
Rollback on verification failure is whole-migration, not per-file: if content-preservation
(AC-002) or census (AC-003) fails for any of the four files, ALL FOUR files' original content is
left completely untouched (fail-loud, not partial-and-silent) — including files that themselves
staged cleanly. No subset of the four files is ever migrated while the others are left pending.

### AC-007 (traces to BC-1.18.013 Postcondition 5, EC-004)
Idempotency has two layers. The governed migration's OWN idempotency signal is `completed.json`'s
presence (`ALREADY_MIGRATED`, exit 0, checked BEFORE any lock is acquired) — authoritative for
"has this governed migration already run." Independently, each of the four files' OWN
`mechanism_a_backfill_already_migrated` / Backfill Recovery Manifest presence check
(BC-1.18.008 Invariant 3) remains the per-file idempotency basis the binary consults while
performing each file's split, including on resume-from-STAGING (AC-011/EC-003): the binary MUST
re-run BC-1.18.008's own per-file idempotency check for each of the four files rather than
assuming none have a pre-existing manifest.

### AC-008 (traces to BC-1.18.013 Postcondition 6, Invariant 5)
The migration operates, in ONE invocation, on EXACTLY the four canonical
`v1.0-brownfield-backfill` files (AC-001's fixed set) — never a wildcard, never a different
cycle, without a separate ADR-052 allowlist amendment. `validate_write_target()` verifies the
resolved canonical path matches EXACTLY one of these four; `.factory/cycles/**` is explicitly NOT
an accepted pattern. Other cycles' oversized append-log files (e.g.
`v1.0-feature-engine-discipline-pass-1/`) are OUT OF SCOPE for this story's Task T-10 and require
a separate ADR-052 allowlist amendment plus a separate armed-activation manifest.

### AC-009 (traces to BC-1.18.013 Postcondition 7, Invariant 4)
This migration's completion DOES gate BC-7.08.001's Cohort-B fail-closed flip (the inverse of
BC-1.18.011's B2 migration, which explicitly does NOT gate that flip). An implementer or
architect closing out BC-7.08.001's own gate check MUST verify `completed.json` exists for
`backfill-append-logs`, not merely that BC-1.18.008's algorithm exists in the codebase.

### AC-010 (traces to BC-1.18.013 Postcondition 8)
ShardRegistry enrollment (Layer 2 closure) is performed as a SEPARATE, ordinary `Edit`/`Write`
step — NOT part of the migration binary's ADR-052 §Decision 8 write-target allowlist (the
migration binary does not, and is not authorized to, write `.factory/shard-config.toml`).
Enrollment adds one `[[shard]]` entry per file using BC-1.18.005's existing schema
(`artifact_stem`, `artifact_path`, `shard_cap_bytes`, `shape = "flat"`, the four
`cap_formula_inputs` fields), performed after `completed.json` is written so the enrolled cap
check does not race an in-flight backfill. **Delivery-time precondition (implementer-visible,
does not block this story's own dispatch-readiness):** `.factory/shard-config.toml` MUST be a
registered `artifact_type` in `plugins/vsdd-factory/config/artifact-path-registry.yaml` before
this enrollment write is attempted, or the `validate-artifact-path` PreToolUse gate rejects it —
a pre-existing gap already identified by ADR-053 for STORY-INDEX's own sharding effort, routed to
devops-engineer/architect, not product-owner's or this story's implementer's to resolve
unilaterally.

### AC-011 (traces to BC-1.18.013 EC-002, EC-003)
If the migration crashes after completing canonical path moves for some but not all of the four
files (post-pointer-swap), ADR-052 §Decision 7c step 7's forward recovery resumes from the intent
log's first uncompleted move (matching-destination-hash rule); already-moved files are NOT
re-moved. If a prior activation attempt built a fully-staged generation for all four files but
crashed before the pointer swap, re-running detects txn record state STAGING and MUST re-run the
FULL per-file census (AC-002/AC-003) for all four files before proceeding — it does NOT skip
re-verification merely because a staged generation was previously built.

### AC-012 (traces to BC-1.18.013 EC-005)
If one of the four canonical files does not exist, or is unreadable, at activation time, the
migration ABORTS before acquiring the flock or building any staging generation (fails the
pre-lock/under-exclusion validation, ADR-052 §Decision 4b/4c) with a clear non-zero exit; no
partial state is written for any of the four files.

### AC-013 (traces to BC-1.18.013 EC-006)
If a single record within one of the four files exceeds `shard_cap_bytes` on its own, this is NOT
a migration-level abort: BC-1.18.008's own oversized-record exception (`oversized_record: true`)
applies to that file's shard exactly as BC-1.18.008 specifies; the governing census-agreement gate
(AC-002/AC-003) tolerates the flagged exception and proceeds normally with the other files and
that file's remaining shards.

### AC-014 (traces to BC-1.18.013 EC-007)
If `record_boundary_offsets` computed from the marker regex yields zero boundaries for one of the
four files despite non-empty content, this is a hard failure via BC-1.18.008 Postcondition 6's
fail-loud content-preservation gate for that file, surfaced by this migration as
`CONTENT_PRESERVATION_ABORT` (exit 2) for the WHOLE migration per AC-006 — never a silent
empty-oracle partition.

CHANGELOG delivery (T-11: add a `CHANGELOG.md` `[Unreleased] > Added` entry before the PR is
opened) remains a mandatory Task per the story-writer contract; it is not itself BC-traced and is
tracked in the Tasks section, not as a numbered AC.

## Architecture Mapping

| Component | Module | Pure/Effectful | Notes |
|-----------|--------|---------------|-------|
| CLI subcommand dispatch | `crates/factory-dispatcher/src/main.rs` (`backfill-append-logs` subcommand) | Effectful | RESOLVED 2026-09-25 per ADR-052 §Decision 2: `factory-dispatcher`, co-located with `shard_manager.rs`. `last-amended-migrate` is NOT the host binary. |
| Marker-regex computation for append-log stems | New module, e.g. `append_log_markers.rs` | Pure | Maps artifact stem -> `record_boundary_offsets`; unit-testable without disk I/O |
| Backfill executor (existing, reused) | `crates/factory-dispatcher/src/shard_manager.rs`: `run_mechanism_a_backfill_split` | Effectful (disk I/O) | Already implemented; this story WIRES it, does not reimplement it |
| Multi-file governed-migration atomicity envelope | `crates/factory-dispatcher/src/executor.rs` (advisory flock, durable txn record, framed intent log, `CURRENT.json` pointer swap, `completed.json`) | Effectful (disk I/O) | NEW per ADR-052 §Decision 7a/7b/7c; wraps four independent per-file `run_mechanism_a_backfill_split` invocations (BC-1.18.013 Invariant 1) |
| ShardRegistry enrollment entries | `.factory/shard-config.toml` (ordinary Edit/Write, NOT part of the migration binary's write-target allowlist) | Pure (config data) | Enrolls the four files for BC-1.18.005 size-trigger (BC-1.18.013 Postcondition 8); see AC-010's registry-precondition note |
| Content-preservation census | `shard_manager.rs`: `mechanism_a_verify_backfill_content_preserved` | Pure (given content) | Already implemented; reused |

Architecture section files that govern this story:
- `architecture/decisions/ADR-051-layer-2-two-mechanism-size-triggered-shard-rotation-append-logs-and-bc-index-sharding.md` — the definitive spec for the marker regexes, cap values, and BC-1.18.008 mechanism-A contracts
- `architecture/decisions/ADR-052-native-migration-cli-bash-tool-allowlist-sanctioned-execution-path.md` — RESOLVED binary placement (§Decision 2), invocation mechanism (§Decision 1/3), armed-activation manifest (§Decision 4), native admission gate (§Decision 5a), crash-atomicity envelope (§Decision 7a/7b/7c), and Rule 7 resolution (§Decision 9)
- `architecture/module-decomposition.md` — boundary between `last-amended-migrate` (migration CLI, BC-10.13.001-scoped, unaffected by this story) and `factory-dispatcher` (hook runtime + migration binary host)

## Edge Cases

> Superseded 2026-09-25: this table now mirrors BC-1.18.013's own Edge Cases table (the
> authoritative source once the BC was authored), rather than the story's pre-BC provisional
> edge-case guesses. IDs match BC-1.18.013 EC-001..EC-008 and each is also cited from the
> Acceptance Criteria above (AC-006, AC-011, AC-012, AC-013, AC-014, AC-001).

| ID | Description | Expected Behavior |
|----|-------------|-------------------|
| EC-001 | Postcondition 1 (content-preservation) passes for three files but Postcondition 2 (independent census) fails for the fourth | Migration ABORTS for ALL FOUR files per Postcondition 4 (AC-006) — a per-file failure is a whole-migration failure; the three passing files' original content is also left untouched |
| EC-002 | Migration crashes after completing canonical path moves for 2 of the 4 files (post-pointer-swap, mid-step-7) | ADR-052 §Decision 7c step 7's forward recovery resumes from the intent log's first uncompleted move (matching-destination-hash rule) (AC-011); the 2 already-moved files are NOT re-moved |
| EC-003 | A prior activation attempt built a fully-staged generation for all four files but crashed before the `CURRENT.json` pointer swap | Re-running MUST detect txn record state STAGING and resume toward the pointer swap, re-running the FULL per-file census for all four files before proceeding (AC-011) |
| EC-004 | `backfill-append-logs` is invoked after a prior activation already reached `completed.json` | `ALREADY_MIGRATED` (exit 0); no lock acquired; zero filesystem mutation (AC-007) |
| EC-005 | One of the four canonical files does not exist, or is unreadable, at activation time | The migration ABORTS before acquiring the flock or building any staging generation (AC-012); no partial state is written |
| EC-006 | A single record within one of the four files exceeds `shard_cap_bytes` on its own | NOT a migration-level abort: BC-1.18.008's own oversized-record exception applies; the census gate tolerates it and proceeds (AC-013) |
| EC-007 | `record_boundary_offsets` yields zero boundaries for one of the four files despite non-empty content | Hard failure via BC-1.18.008 Postcondition 6's fail-loud gate, surfaced as `CONTENT_PRESERVATION_ABORT` (exit 2) for the WHOLE migration (AC-014) |
| EC-008 | `backfill-append-logs` invoked with a path argument, a `--cycle` flag, or any token outside the closed grammar | REJECTED by the pre-shell classifier before the binary is even invoked; rejected again by the binary itself if bypassed (AC-001) |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `append_log_markers` (new) | pure-core | Maps artifact stem to record-boundary offsets; no disk I/O, only regex computation over known BC-1.18.008 patterns |
| CLI subcommand dispatch (`factory-dispatcher/src/main.rs`) | effectful-shell | Reads argv (closed grammar only), calls effectful I/O functions, writes output to stdout |
| Multi-file governed-migration envelope (`executor.rs`) | effectful-shell | Advisory flock, txn record, intent log, atomic renames, fsync/F_FULLFSYNC durability barriers — all disk I/O |
| `run_mechanism_a_backfill_split` (existing, reused) | effectful-shell | Reads source file, writes shard files, publishes shard index — all disk I/O |
| `record_boundary_offsets_are_well_formed` (existing, reused) | pure-core | Pure predicate over byte-slice and offset array; no I/O |

## Behavioral Contracts

`BC-1.18.013` is this story's primary anchor (`behavioral_contracts: [BC-1.18.013]`); every AC
above traces to one of its clauses. The remaining BCs are prior-art / lineage that BC-1.18.013
invokes or is modeled on and that the implementer MUST load and understand before any
implementation begins.

| BC | Version | Role |
|----|---------|------|
| BC-1.18.013 | v1.1 | **PRIMARY ANCHOR.** Governed one-time migration contract for mechanism A's backfill-split of the four append-log files, activated via ADR-052's sanctioned execution path (`backfill-append-logs`). Specifies the closed argument grammar, four-file all-or-nothing atomicity, two-layer idempotency, ShardRegistry-enrollment precondition, and the BC-7.08.001 gating relationship. This story implements it end-to-end. |
| BC-1.18.008 | v1.10 | Prior-art. Mandates the one-time backfill-split and specifies marker regexes, Backfill Recovery Manifest format, content-preservation requirements, and idempotency invariants for the four append-log files. BC-1.18.013's governed migration invokes `run_mechanism_a_backfill_split` unmodified (v1.10 adds the reciprocal Related-BCs cross-reference to BC-1.18.013, product-owner, same burst). |
| BC-1.18.005 | v1.15 | Prior-art. Specifies the cap formula, native size-trigger, and `[[shard]]`/`ShardEntry` schema that BC-1.18.013 Postcondition 8's enrollment populates. |
| BC-1.18.006 | v1.12 | Prior-art. Specifies the roll mechanism (cap-triggered shard creation) that fires automatically once enrollment is complete; also the atomic-write/seal primitives BC-1.18.008 reuses. |
| BC-1.18.011 | v1.10 | Prior-art. The B2 sibling governed-migration BC (BC-INDEX body split) that BC-1.18.013's structure is modeled directly on, substituting four independent files for one logical body's ten-way partition. |
| BC-10.13.001 | v1.5 | Prior-art. Governs the `last-amended-migrate` CLI's target-file scope (5 files) and mutation contract. UNAFFECTED by this story — the new subcommand lives in `factory-dispatcher` per ADR-052 §Decision 2, not `last-amended-migrate`; BC-10.13.001's scope is neither extended nor modified. |

## Token Budget Estimate

| Item | Estimated Tokens |
|------|-----------------|
| Story spec (this file, v1.2) | ~11,000 |
| BC files (1 BC) — BC-1.18.013 v1.1 (primary anchor) | ~7,000 |
| Lineage BCs read at activation (BC-1.18.008 v1.10, BC-1.18.005 v1.15, BC-1.18.006 v1.12, BC-1.18.011 v1.10 — structural template for the governed-migration pattern, BC-10.13.001 v1.5) | ~34,000 |
| VP files (VP-143, VP-144, VP-145) | ~4,500 |
| ADR-051 (the marker-regex source of truth) | ~15,000 |
| ADR-052 (sanctioned execution path: binary placement, closed grammar, armed-activation manifest, native admission gate, crash-atomicity envelope, Rule 7 resolution) | ~22,000 |
| `shard_manager.rs` (relevant sections: ~2,500 lines of the ~7,500 total) | ~20,000 |
| `executor.rs` (relevant sections: admission gate, flock, txn record, intent log, pointer swap) | ~15,000 |
| `error-taxonomy.md` | ~8,000 |
| Backfill tests (`bc_1_18_008_backfill_split_test.rs`) | ~10,000 |
| New test file (fault-injection + differential + grammar-rejection fixtures for VP-143/144/145) | ~10,000 |
| **Total estimate** | **~156,500** |
| Agent context window (200,000 tokens typical) | ~78% utilization |

Above the 20-30% split-required threshold in raw sum, but the lineage BCs/ADRs/executor.rs
sections are read ONCE at activation and cached across the implementation session, not re-read
per task; the per-task working set (BC-1.18.013 + the specific file(s) under edit + the
corresponding test file) stays within ~40-50k tokens per Task T-6..T-10. No further story split
is warranted: BC-1.18.013's four-file atomicity unit is not independently decomposable without
breaking the single-migration-binary Spec-First Gate this story exists to close.

## Tasks

- [x] T-1: Architect activation decision — RESOLVED 2026-09-25 per ADR-052 §Decision 2: the new
  CLI subcommand lives in `crates/factory-dispatcher/` (co-located with `shard_manager.rs`), NOT
  `last-amended-migrate`.
- [x] T-2: Product-owner authored BC-1.18.013 v1.1 specifying the executor contract, enrollment
  precondition, activation-step sequence, and BC-7.08.001 gating relationship (Spec-First Gate
  Gate 1 SATISFIED, 2026-09-25).
- [x] T-3: Architect allocated VP-143 (integration), VP-144 (proptest), VP-145
  (integration/safety) for BC-1.18.013's key postconditions; registered in VP-INDEX v3.24 and
  verification-architecture.md v1.37 (Spec-First Gate Gate 2 SATISFIED, 2026-09-25).
- [x] T-4: `behavioral_contracts: [BC-1.18.013]` and `verification_properties: [VP-143, VP-144,
  VP-145]` frontmatter updated in this burst; status transitioned draft -> ready (Spec-First
  Gate S-7.01 satisfied).
- [ ] T-5: Test-writer: Write Red Gate test stubs for the `backfill-append-logs` subcommand's
  postconditions. Tests MUST reference `BC-1.18.013` and cover: closed-grammar rejection
  (AC-001/VP-145), per-file content-preservation and census delegation (AC-002/AC-003/VP-144),
  four-file all-or-nothing atomicity and the TOCTOU fingerprint recheck (AC-004/AC-005/VP-143),
  whole-migration rollback (AC-006), two-layer idempotency (AC-007), fixed four-file scope
  (AC-008), the BC-7.08.001 gating check (AC-009), ShardRegistry enrollment (AC-010), and
  EC-001 through EC-008 (AC-011..AC-014).
- [ ] T-6: Implementer: Stub the `backfill-append-logs` subcommand and the per-file
  `run_mechanism_a_backfill_split` call sites (compilable stubs, `todo!()` bodies), satisfying
  Red Gate density check >= 0.5.
- [ ] T-7: Implementer: Implement `append_log_markers` (or equivalent) pure module mapping
  each artifact stem to its `record_boundary_offsets` using BC-1.18.008's marker regexes.
  Unit tests must cover all four stems and the EC-007 empty-oracle guard (AC-014).
- [ ] T-8: Implementer: Wire the multi-file governed-migration envelope in `executor.rs`
  (advisory flock, durable txn record, framed intent log, single `CURRENT.json` pointer swap,
  `completed.json`) around four independent `run_mechanism_a_backfill_split` invocations per
  ADR-052 §Decision 7a/7b/7c. Integration tests confirm AC-004/AC-005/AC-006/AC-011.
- [ ] T-9: Implementer: Add the ShardRegistry `[[shard]]` enrollment entries for the four
  append-log files to `.factory/shard-config.toml` as a separate ordinary Edit/Write step
  (AC-010), AFTER `completed.json` exists. Confirm the `.factory/shard-config.toml`
  artifact-path-registry precondition (AC-010) is satisfied before attempting the write; if not,
  route to devops-engineer/architect per ADR-053. Confirm that after enrollment, a synthetic
  over-cap write triggers a BC-1.18.006 roll in the test harness.
- [ ] T-10: Implementer: Execute the one-time backfill against the live
  `v1.0-brownfield-backfill/` cycle files (the activation step; AC-001/AC-008 scope exactly
  these four files, never a wildcard, never `v1.0-feature-engine-discipline-pass-1/`). Confirm
  all four files pass content-preservation and census (AC-002/AC-003); emit the final census
  report to stdout. Gate: no `CONTENT_PRESERVATION_ABORT`/`FINGERPRINT_MISMATCH_ABORT`; record
  counts match BC-1.18.008's table-row counts from the BC's own measurement; `completed.json`
  written (AC-009 dependency for BC-7.08.001).
- [ ] T-11: Add a CHANGELOG entry under `[Unreleased] > Added` in `CHANGELOG.md` describing
  the new CLI subcommand and ShardRegistry enrollment behavior before opening the PR.

## Previous Story Intelligence

S-25.05 (Rotate Changelog Crash-Atomicity) is a direct sibling in this epic: it addresses the
`rotate_changelog_at` cross-file crash-atomicity gap discovered in S-25.02 cluster-4. The pattern
repeats here: S-25.02 cluster-3 implemented an algorithm that has no wired production call site.
Prior lessons from S-25.02 cluster-3 delivery apply directly:

- **Topological input-hash recompute order (L-BB-D1196-cascade-input-hash-topological):** When
  multiple files are changed in the same burst, always recompute input hashes in dependency order:
  BC files first, then error-taxonomy if BC-dependent, then the story file last. Running
  `compute-input-hash --update` in wrong order produces stale values that drift.
- **BC array propagation (POLICY 8):** Every change to `behavioral_contracts:` frontmatter must
  propagate atomically to the story's BC table, AC traces, and Token Budget subtable in the same
  commit burst.
- **Error taxonomy display drift (F-C4-ADV-001 class, 6th+ recurrence):** S-12.13 was anchored
  for a lint gate diffing every E-SHD-emitting Rust source's Display implementations against
  error-taxonomy.md's Message Format column. If the new executor introduces a new E-xxx error
  code, its Display string MUST match the taxonomy's Message Format cell exactly. Verify with
  S-12.13's lint gate before PR.
- **BC-5.39.001 3-CLEAN convergence:** No story in E-25 has reached 3-CLEAN on its first
  adversarial pass. Budget at least 3 adversary passes before PR readiness.
- **Spec-First Gate S-7.01:** Do not move to implementation without the new BC's postconditions
  in hand. S-25.02's cluster-3 adversary convergence was prolonged precisely because behavioral
  details were discovered iteratively during review rather than pre-specified.

## Local Risk Register (FINDING MED-3 — 2026-09-12)

These risks were identified in the adversarial direction review. They are referenced from the
`risk_mitigations:` frontmatter array as R-S25.06-001, R-S25.06-002, R-S25.06-003.

| Risk ID | Description | Impact | Mitigation |
|---------|-------------|--------|-----------|
| R-S25.06-001 | Append-logs grow every burst through clusters 5-7 while validation is fail-open. `validate-burst-log` PostToolUse on_error=continue → validation silently LOST on oversized files. | HIGH — each new burst adds unvalidated content to already-oversized files, deepening fuel-exhaustion exposure and the CWE-754 silent-PASS pattern | This story is P1; backfill-split + ShardRegistry enrollment must complete before the W3 gate closes. Failure to ship S-25.06 before cluster-7 dispatch means the Cohort-B flip is activated without the underlying size problem having been resolved. |
| R-S25.06-002 | DEFAULT_FUEL_CAP 20M is NOT operative at operator level (rc.23 embeds 10M). CLAUDE.md: "20M does not eliminate exhaustion for large cycle artifacts." Growth risk remains live through all clusters 5-7 and beyond. Until ShardRegistry enrollment fires, every future write to `burst-log.md` or `decision-log.md` bypasses the cap check. | HIGH — fuel exhaustion → fail-open → unvalidated state; a systemic loop that continues until sharding is active | AC-010 (renumbered from provisional AC-005, 2026-09-25) ShardRegistry enrollment activates BC-1.18.005 cap-triggered rotation after the one-time backfill, capping future writes. This is the permanent mechanical mitigation. Operator-level 20M fuel-cap (rc.24) will additionally reduce exhaustion frequency but is not a substitute for sharding. |
| R-S25.06-003 | If mutation tooling (cargo mutants / mutagen) cannot handle further growth of shard_manager.rs before this story ships, mutation testing falls back to manual boundary-value analysis — a quality regression from the production-grade standard. | MEDIUM — affects test quality of the sharding implementation; does not block execution directly | Stop condition is the existing Red Gate + 3-CLEAN convergence gate for this story's PR. If mutation tooling cannot complete within CI time bounds, the PR gate blocks. No separate escape valve is needed — the gate IS the stop condition. |

## Architecture Compliance Rules

These rules are extracted from the architecture section files and ADRs that govern this story.
The implementer MUST NOT violate any of these; violations are bugs, not style preferences.

1. **No WASM-sandboxed executor for append-log migration (ADR-051 §Decision 7 / CLAUDE.md §WASM
   plugin fuel budgets):** The executor MUST run as a native binary (not inside a WASM plugin,
   not from within a hook plugin). WASM fuel budgets are insufficient for files in the 1 MB
   range. A WASM executor for this task is a bug, not a design choice.

2. **Single-commit-per-burst (TD-VSDD-053):** The activation-step execution (T-10) produces
   four modified cycle files. These MUST land in a single `.factory/` commit (state-manager
   disciplines). Never split the four-file compaction across two commits.

3. **Shard-manager is the single mutation authority (ADR-051 §Scope Note):** Do NOT add a
   second implementation of the backfill-split algorithm. The ONLY correct path is calling
   `run_mechanism_a_backfill_split` from `shard_manager.rs`. Any reimplementation is a POLICY
   violation.

4. **Enrollment is per-cycle-directory (ADR-051 §canonical-filename convention):** The
   ShardRegistry entry for `decision-log.md` must use the full path under a specific cycle
   directory (e.g. `.factory/cycles/v1.0-brownfield-backfill/decision-log.md`), not a glob
   or wildcard. Each cycle's files are enrolled independently with that cycle's configured cap.

5. **`last-amended-migrate`'s TARGET_FILES is unaffected (BC-10.13.001; RESOLVED 2026-09-25):**
   ADR-052 §Decision 2 places the `backfill-append-logs` subcommand in `factory-dispatcher`, NOT
   `last-amended-migrate`. BC-10.13.001's 5-file TARGET_FILES scope is therefore neither extended
   nor amended by this story — no BC-10.13.001 amendment is needed. (Previously this rule flagged
   a conditional BC-10.13.001 amendment pending the T-1 binary decision; that condition did not
   materialize.)

6. **`run_mechanism_a_backfill_split` signature is stable (BC-1.18.008 anchor):** Do not modify
   the existing function signature. If the new CLI path needs a slightly different invocation
   shape, add a wrapper. Do not modify the existing function, which is already anchored by
   BC-1.18.008 and covered by `bc_1_18_008_backfill_split_test.rs` (73+ passing tests).

7. **POL-3 no-bypass discipline (TD-FACTORY-HOOK-BYPASS-001 P0) — RESOLVED per ADR-052 §Decision 9
   (2026-09-25; FINDING HIGH-2 CLOSED).** This rule's original text asserted an impossible
   combination of constraints (a "native CLI" that must ALSO be logged as an Edit/Write tool
   call — a native binary performing filesystem writes cannot simultaneously be an Edit/Write
   tool call). Per ADR-052 §Decision 9, the architect withdrew that original text and replaced it
   verbatim with:

   > "The activation step is executed via the `Bash` tool with one-time interactive human
   > approval at F4 activation (no standing settings.json allowlist). The migration binary is
   > invoked at its absolute trusted path:
   > `{project-root}/target/release/factory-dispatcher backfill-append-logs`. The invocation is
   > NOT an Edit/Write tool call. It is a Bash execution of the native migration binary under
   > ADR-052 §Decision 3's closed argument grammar, activated under ADR-052 §Decision 4's
   > armed-activation manifest, producing a census report captured per ADR-052 §Decision 6."

   **Resolution addressed:** (a) Agent tool surface for T-10 is the `Bash` tool, invoking the
   native binary at its absolute trusted path — never Edit/Write, never a raw `cp`/`python -c`
   bypass. (b) POL-3/TD-FACTORY-HOOK-BYPASS-001 is satisfied because the invocation goes through
   the closed-argument-grammar pre-shell classifier (ADR-052 §Decision 5c) and the native
   admission gate in `executor.rs` (ADR-052 §Decision 5a) — not a raw, unvalidated shell write.
   (c) The audit trail is the stdout census report (ADR-052 §Decision 6, NIST AU-9) plus the
   durable txn record and `completed.json`. ADR-052 was authored with S-25.06 explicitly as an
   input and was human-ratified 2026-09-20 (D-1232, POLICY 22). No further architect action is
   required for this gate; `status: ready` reflects this resolution.

## Library & Framework Requirements

| Library / Crate | Version Pin | Source of Truth | Notes |
|-----------------|-------------|-----------------|-------|
| `factory-dispatcher` crate | workspace member | `Cargo.toml` workspace | RESOLVED host (ADR-052 §Decision 2): the `shard_manager` module AND the new `backfill-append-logs` subcommand both live here |
| `clap` | workspace-pinned version | `Cargo.toml` workspace | CLI argument parsing; match existing subcommand pattern in `factory-dispatcher/src/main.rs`; grammar is CLOSED (AC-001) — no free-form arg definitions |
| `serde` / `toml` | workspace-pinned versions | `Cargo.toml` workspace | ShardIndex (de)serialization already uses these; `.factory/shard-config.toml` enrollment (AC-010) uses the same `toml` pin; do not add new versions |
| `sha2` | workspace-pinned version | `Cargo.toml` workspace | Used by `run_mechanism_a_backfill_split` for `original_sha256` seal AND by the whole-corpus `source_sha256` fingerprint (AC-005); do not pin a different version |

`last-amended-migrate` is NOT a dependency of this story (RESOLVED 2026-09-25, ADR-052 §Decision 2)
— it remains BC-10.13.001-scoped and untouched.

Do NOT introduce new dependencies. All needed primitives (`run_mechanism_a_backfill_split`,
atomic-write, census functions) are already in the workspace. Any new external dependency
requires architect approval.

## File Structure Requirements

Files to CREATE:
| File | Module | Notes |
|------|--------|-------|
| `crates/factory-dispatcher/src/append_log_markers.rs` | Pure | Maps artifact stem -> `record_boundary_offsets` using BC-1.18.008 marker regexes; unit-tested without disk I/O |
| `crates/factory-dispatcher/tests/s2506_append_log_backfill_test.rs` | Integration tests | Red Gate tests for AC-001..AC-014 + EC-001..EC-008; stems from `bc_1_18_008_backfill_split_test.rs` pattern; includes VP-143 fault-injection, VP-144 differential, VP-145 grammar-rejection fixtures |

`crates/factory-dispatcher/` is the RESOLVED host binary (T-1 closed, ADR-052 §Decision 2) —
co-located with `shard_manager.rs`. `last-amended-migrate` is NOT used.

Files to MODIFY:
| File | Change | Notes |
|------|--------|-------|
| `crates/factory-dispatcher/src/main.rs` | Add new `backfill-append-logs` subcommand (closed grammar: `backfill-append-logs` / `backfill-append-logs --census`) and dispatch to new module | Match existing CLI patterns; use `clap` derive macros; AC-001 |
| `crates/factory-dispatcher/src/executor.rs` | Add the multi-file governed-migration envelope: advisory flock, durable txn record, framed intent log, `CURRENT.json` pointer swap, `completed.json`, native admission gate scoped to `.factory/cycles/` | ADR-052 §Decision 5a/7a/7b/7c; AC-004/AC-005/AC-006/AC-011 |
| `crates/factory-dispatcher/src/lib.rs` | Export `append_log_markers` module | |
| `.factory/shard-config.toml` | Add four `[[shard]]` enrollment entries (AC-010) | Separate ordinary Edit/Write step, AFTER `completed.json` exists; precondition: `artifact-path-registry.yaml` entry for `shard-config.toml` (routed to devops-engineer/architect per ADR-053 if absent) |
| `CHANGELOG.md` | Add `[Unreleased] > Added` entry (T-11) | Before PR opens |
| `.factory/stories/S-25.06-append-log-backfill-split-executor.md` | `behavioral_contracts:`, `verification_properties:`, `input-hash` updated THIS burst (v1.2) | State-manager to recompute input-hash |

Files to NOT modify:
- `crates/factory-dispatcher/src/shard_manager.rs` — existing function signatures are stable
- `crates/last-amended-migrate/src/migrate.rs` — TARGET_FILES const and migrate subcommand are BC-10.13.001 scoped; do not expand them
- Any `.factory/cycles/*/` files directly via shell/Python (use the sanctioned executor only)
