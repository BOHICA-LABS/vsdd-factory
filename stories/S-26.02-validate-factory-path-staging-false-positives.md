---
document_type: story
level: ops
story_id: "S-26.02"
epic_id: "E-26"
version: "1.0"
status: draft
# BC status: pending PO authorship
producer: story-writer
timestamp: "2026-09-19T00:00:00Z"
phase: 2
inputs:
  - .factory/specs/behavioral-contracts/ss-04/BC-4.16.001.md
  - crates/hook-plugins/validate-factory-path-staging/src/lib.rs
  - .factory/stories/S-21.01-validate-factory-path-staging.md
input-hash: "ac93252"
traces_to: .factory/stories/epics/E-26-post-rc25-hook-hardening.md
points: 5
depends_on: []
blocks: [S-26.05]
behavioral_contracts: []
verification_properties: []
priority: "P1"
cycle: v1.0-feature-post-rc25-hook-hardening
wave: 1
target_module: "crates/hook-plugins/validate-factory-path-staging"
subsystems: [SS-04]
estimated_days: 2
assumption_validations: []
risk_mitigations: []
tdd_mode: strict
closes_issues: [840]
---

> **tdd_mode:** strict — full TDD Iron Law enforced. This is a defect fix in a live,
> shipped hook plugin, not a facade-mode candidate.

> **BC status: pending PO authorship.** `behavioral_contracts: []` is intentional.
> This story CANNOT advance `draft → ready` until product-owner amends BC-4.16.001
> per the Spec-First Gate (S-7.01). See "Required BC Amendment" below.

> **Registration note (story-writer, planning-only dispatch):** This is a STUB
> authored during a planning/authoring-only pass. `input-hash: "ac93252"` is the
> value the `validate-input-hash` PostToolUse guard itself computed and reported at
> write time (story-writer's tool access has no exec/process, so this value was taken
> verbatim from the guard's `block_reason` drift report). State-manager should still
> re-run `compute-input-hash S-26.02-validate-factory-path-staging-false-positives.md
> --update` at the registration burst to confirm/refresh it.

> **Execute:** `/vsdd-factory:deliver-story S-26.02` (blocked until BC-4.16.001
> amendment lands and `status` advances to `ready`)

# S-26.02: `validate-factory-path-staging` False-Positives on Raw Command String / Branch Name

## Narrative
- **As an** agent staging changes on a `develop`-based branch
- **I want to** `validate-factory-path-staging` to gate on what is ACTUALLY staged
  under `.factory/`, not on whether my command's raw text happens to mention a
  `.factory/` path token
- **So that** I am never blocked from legitimate git operations (including a
  `gh issue create` whose BODY text merely references a path) when zero factory paths
  are staged

## Problem Statement (GH issue #840, P1, AXIS-2 current-source defect)

`validate-factory-path-staging` (`crates/hook-plugins/validate-factory-path-staging/`,
`PreToolUse`; BC-4.16.001; registered per ADR-031 §Decision 3, story S-21.01) exists to
enforce that `.factory/`-tree paths are staged only via the correct factory-artifacts
worktree/branch discipline (CAP-034). The guard was observed raising
`FactoryPathOnProductBranch` in two false-positive shapes:

1. Blocking a git-staging operation on a `develop`-based branch when the actual staged
   diff (`git diff --cached --name-only`) contains **zero** `.factory/`-tree paths —
   the guard decided from the raw command STRING (a path token appearing anywhere in
   the invoked command text) and the branch name alone, not from what was actually
   staged.
2. Blocking a `gh issue create` call whose issue BODY text merely mentioned a
   `.factory/` path as a string literal (describing this very defect, in fact) — the
   guard's raw-string scan fired on prose, not on any git-staging action at all.

## Required BC Amendment (product-owner routing — NOT authored by this story)

BC-4.16.001 needs its gate predicate corrected from "raw command string contains a
`.factory/` path token AND branch is `develop`-based" to "the ACTUAL currently-staged
path set (`git diff --cached --name-only`, or the tool-call's own destination path for
non-git tool calls) contains at least one path under `.factory/`." Product-owner must
also decide, in consultation with architect, whether non-git tool calls (e.g.
`gh issue create`) should be exempted from this guard entirely (they never stage
anything) or should have their own narrower, correctly-scoped check — the story-writer
flags this ambiguity for PO/architect adjudication rather than resolving it here.

## Acceptance Criteria (DRAFT — pending BC-4.16.001 amendment; NOT yet BC-traced)

### AC-001 (BC trace: pending BC-4.16.001 amendment)
- On a `develop`-based branch, a git-staging command whose `git diff --cached
  --name-only` output contains zero paths under `.factory/` is NEVER blocked with
  `FactoryPathOnProductBranch`, regardless of what path tokens appear in the raw
  command string.
- **Test:** `test_BC_4_16_001_zero_staged_factory_paths_not_blocked()`

### AC-002 (BC trace: pending BC-4.16.001 amendment)
- Reproduces GH issue #840's exact `gh issue create` false-positive as a regression
  test: a non-staging tool call whose text body mentions a `.factory/` path string is
  NOT blocked.
- **Test:** `test_BC_4_16_001_gh_issue_create_mention_not_blocked_regression()`

### AC-003 (BC trace: pending BC-4.16.001 amendment)
- A git-staging command whose `git diff --cached --name-only` output DOES contain at
  least one path under `.factory/`, on a `develop`-based branch, is still correctly
  blocked (no regression on the guard's true-positive case; CAP-034 enforcement
  intact).
- **Test:** `test_BC_4_16_001_true_positive_staged_factory_path_still_blocked()`

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| validate-factory-path-staging plugin | `crates/hook-plugins/validate-factory-path-staging/src/lib.rs` | effectful-shell (`host::exec_subprocess` for `git diff --cached`) |
| Staged-path predicate (new/amended) | `crates/hook-plugins/validate-factory-path-staging/src/lib.rs` (or extracted pure fn) | pure-core (given an injected staged-path list) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `git diff --cached --name-only` subprocess call itself fails (e.g., not a git repo, sandbox denial) | Fail-open or fail-closed decision must be explicit in the BC amendment — story-writer flags this as a required PO decision, not assumed |
| EC-002 | Staged path is under `.factory/` but via a symlink or relative-path traversal (`../.factory/...`) | Guard's path-normalization must resolve the real path before comparing against the `.factory/` prefix (reuse `path_util::resolve_path_for_allowlist` pattern per BC-2.02.011 precedent, S-19.03) |
| EC-003 | Mixed staging: some paths under `.factory/`, some not, in the same commit | Guard blocks (true positive) — presence of ANY staged `.factory/` path on a product branch is sufficient, per CAP-034's existing intent |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| `crates/hook-plugins/validate-factory-path-staging/src/lib.rs` | effectful-shell | Performs `host::exec_subprocess(["git","diff","--cached","--name-only"])` |
| Path-prefix predicate (staged paths → bool) | pure-core | Given a `Vec<String>` of staged paths, a pure function decides block/allow — testable without a subprocess |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | 1,800 |
| BC-4.16.001 (post-amendment) | 3,500 |
| `validate-factory-path-staging` crate (lib.rs + main.rs + tests) | 3,000 |
| S-21.01 precedent story (original guard story) | 2,500 |
| Tool outputs overhead | 2,000 |
| **Total** | **~12,800** |
| Agent context window | 200K for Sonnet |
| **Budget usage** | **~6%** |

Well within the 20-30% target.

## Tasks (MANDATORY)

1. [ ] Route to product-owner: author BC-4.16.001 amendment (staged-path predicate +
   non-git-tool-call exemption decision) — BLOCKING
2. [ ] Route to architect: adjudicate the EC-001 fail-open-vs-fail-closed question if
   the amendment leaves it open
3. [ ] Rewrite AC-001..AC-003 with concrete `(traces to BC-4.16.001 ...)` citations
   once the amendment lands
4. [ ] Write failing tests (test-writer) for AC-002's regression case
5. [ ] Implement the fix to pass tests (implementer)
6. [ ] Verify purity boundaries (extract the staged-path predicate as a pure function
   if not already separable from the `exec_subprocess` call)
7. [ ] Write edge case tests (EC-001, EC-002, EC-003)
8. [ ] Verify Red Gate (all tests fail before implementation)
9. [ ] Implement (minimum code per test)
10. [ ] Refactor
11. [ ] Add a CHANGELOG entry under `[Unreleased] > Fixed` describing the shipped
    behavior, before creating the PR

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-21.01 | Original `validate-factory-path-staging` guard implementation (BC-4.16.001 v1.0; ADR-031 §Decision 3) | Guard scoped to CAP-034 (factory-path staging discipline); registered as PreToolUse WASM crate | The original scope did not fully specify staged-path-vs-raw-string predicate precision — this gap is exactly what #840 surfaces |
| S-25.04 | Closed a related "Zero-Enforcement Gap" in the same guard (see `.factory/stories/S-25.04-close-validate-factory-path-staging-zero-enforcement-gap.md`) | Established precedent for tightening this guard's predicate without breaking its true-positive coverage | Read S-25.04's implementation before starting — it may already have partially addressed the staged-path-vs-raw-string distinction; verify #840 is not already (partially) fixed there |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| The guard MUST decide from `git diff --cached --name-only` (actual staged state), never from raw command-string pattern matching | BC-4.16.001 (post-amendment), CAP-034 | Code review: assert the block/allow decision function takes a staged-path list as input, not a raw command string |
| Path comparisons MUST use the shared `path_util` resolution pattern (no unresolved relative/symlink traversal) | BC-2.02.011 precedent (S-19.03) | Unit test: symlink/traversal staged path correctly resolves to `.factory/`-prefixed real path |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| wasmtime | 46.0.3 (workspace-pinned) | WASM host runtime |
| wasmtime-wasi | 46.0.3 (workspace-pinned) | WASI shim |

_No new dependencies anticipated._

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `crates/hook-plugins/validate-factory-path-staging/src/lib.rs` | modify | Replace raw-string/branch-name predicate with actual-staged-path predicate |
| `crates/hook-plugins/validate-factory-path-staging/tests/` | modify | Add regression tests for AC-001..AC-003 + EC-001..EC-003 |
| `.factory/specs/behavioral-contracts/ss-04/BC-4.16.001.md` | modify (product-owner, routed) | BC amendment per "Required BC Amendment" above |
| `CHANGELOG.md` | modify | `[Unreleased] > Fixed` entry |

## Changelog

| Version | Date | Author | Change |
|---------|------|--------|--------|
| 1.0 | 2026-09-19 | story-writer | Initial creation (draft stub; planning/authoring-only dispatch). `behavioral_contracts: []` pending BC-4.16.001 amendment. |
