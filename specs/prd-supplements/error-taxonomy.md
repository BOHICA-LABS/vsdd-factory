---
document_type: prd-supplement-error-taxonomy
level: L3
version: "1.17"
status: draft
producer: product-owner
timestamp: 2026-09-11T00:00:00Z
phase: F2
inputs:
  - .factory/specs/prd.md
  - .factory/cycles/v1.0-brownfield-backfill/S-25.02-f2-prd-delta.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.006.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.007.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.008.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.009.md
  - .factory/specs/behavioral-contracts/ss-01/BC-1.18.011.md
input-hash: "412a348"
traces_to: .factory/specs/prd.md
---

# Error Taxonomy: vsdd-factory

> PRD supplement — extracted from PRD §5 (Error Taxonomy). Referenced by: implementer, test-writer.
> Materializes the `.factory/specs/prd-supplements/error-taxonomy.md` reference that `prd.md`
> §5.1/§5b have carried since before this document existed (a pre-existing dangling reference,
> not introduced by S-25.02 — flagged by the S-25.02 F2 PRD delta §7 and closed here per
> F-S2502-F2-006). This file's error categories mirror `prd.md` §5.1's summary table exactly, plus
> the `E-SHD-NNN` category `prd.md` §5.1 does not yet enumerate.

## Error Categories

| Category Code | Category | Description |
|--------------|----------|-------------|
| REG | Registry errors | Hook registry parsing/validation failures (schema version, tool regex, unknown fields) |
| PAY | Payload errors | Malformed or incomplete tool-call payloads reaching the dispatcher |
| CAP | Capability denial | Host-function calls denied by the plugin's declared capability set |
| PLG | Plugin execution | Timeout, crash, or resource-exhaustion outcomes during WASM plugin execution |
| SNK | Sink errors | Event-sink (JSONL/OTLP) write or routing failures |
| ACT | Activation errors | Platform/binary/hooks.json activation failures |
| HK | Hook gate blocks | PreToolUse gate blocks (secrets, destructive commands, branch protection) |
| SHD | Shard management errors | Layer-2 artifact-sharding failures (rotation, backfill, migration) — native dispatcher `HookResult::Error` outcomes, not process exit codes |

## Error Catalog

| Error Code | Category | Severity | Exit Code / HookResult | Message Format |
|-----------|----------|----------|-----------------------|---------------|
| E-REG-001 | Registry errors | broken | exit 0 | `schema_version mismatch: expected <N>, found <M>` |
| E-REG-002 | Registry errors | broken | exit 0 | `invalid tool regex in entry <name>: <regex-error>` |
| E-REG-003 | Registry errors | broken | exit 0 | `unknown field <field> in hooks-registry.toml entry <name>` |
| E-PAY-001 | Payload errors | broken | exit 0 | `missing event_name in payload` |
| E-PAY-002 | Payload errors | broken | exit 0 | `invalid JSON in tool-call payload: <parse-error>` |
| E-CAP-001 | Capability denial | blocked | exit 2 | `exec_subprocess denied: capability not declared for plugin <name>` |
| E-CAP-002 | Capability denial | blocked | exit 2 | `shell-bypass not acknowledged for command <cmd>` |
| E-CAP-003 | Capability denial | blocked | exit 2 | `setuid refused for plugin <name>` |
| E-PLG-001 | Plugin execution | degraded | exit 0 | `plugin <name> timeout (epoch budget exceeded)` |
| E-PLG-002 | Plugin execution | degraded | exit 0 | `plugin <name> timeout (fuel budget exceeded: cap=<N>, consumed=<M>)` |
| E-PLG-003 | Plugin execution | degraded | exit 0 | `plugin <name> crashed (trap): <trap-reason>` |
| E-SNK-001 | Sink errors | degraded | non-blocking | `sink <name> queue full, event dropped` |
| E-SNK-002 | Sink errors | degraded | non-blocking | `sink <name> write failure: <io-error>` |
| E-SNK-003 | Sink errors | degraded | non-blocking | `unknown sink driver type <type>` |
| E-ACT-001 | Activation errors | broken | non-zero | `unsupported platform <platform>` |
| E-ACT-002 | Activation errors | broken | non-zero | `dispatcher binary missing for platform <platform>` |
| E-ACT-003 | Activation errors | broken | non-zero | `hooks.json write failure: <io-error>` |
| E-HK-001 | Hook gate blocks | blocked | exit 2 | `secret detected in <path>: <pattern-name>` |
| E-HK-002 | Hook gate blocks | blocked | exit 2 | `destructive command blocked: <command>` |
| E-HK-003 | Hook gate blocks | blocked | exit 2 | `branch protection violation: <branch>` |
| E-SHD-001 | Shard management errors | broken | `HookResult::Error` | `E-SHD-001: shard-seal-write failure for artifact_stem "<artifact>" — canonical file left in its exact pre-roll state, still over cap: <io-error>` (CORRECTED, S-25.02 cluster-2 LOCAL adversary pass-10, F-C2-P10-001, MINOR: this column previously documented the stale, non-matching `shard-seal-write failure for <artifact>: <io-error>` wording — missing the `E-SHD-001:` code prefix, the `artifact_stem "..."` quoting convention, and the "— canonical file left in its exact pre-roll state, still over cap" clause the shipped `ShardRollError::SealWriteFailed` `Display` impl (`crates/factory-dispatcher/src/shard_manager.rs`) actually emits. BC-1.18.006 EC-003, Postcondition 1 steps (a)-(b); disk-full/permission error mid-roll, canonical file left untouched — no rename occurs under the copy-then-atomic-truncate-in-place mechanism, BC-1.18.006 v1.2) |
| E-SHD-002 | Shard management errors | broken | `HookResult::Error` | THREE distinct real emissions under this ONE code (form (c) added this amendment, F-C3-P9-005, S-25.02 cluster-3 fresh-eyes PR #831 reviewer finding, MINOR — the exhaustiveness attestation gap accompanying BLOCKING-1; forms (a)/(b) unchanged from the prior F-C3-P9-002 sweep): **(a)** `ShardRetentionError::IndexUnavailable` — the retention check's own load of `<artifact-stem>.shard-index.toml` failed (missing in an unexpected way, or malformed TOML) at the moment a retention/compaction check would run — `E-SHD-002: shard-index missing or corrupt for artifact_stem "<artifact>": <io-error>` (BC-1.18.007 EC-005); **(b)** `ShardRetentionError::ArchivalMoveFailed` — the archival move of an already-identified overflow shard from the cycle root to `archive/<artifact-stem>/` failed; no shard-index change is applied for a failed move (same-invocation-atomicity composition with BC-1.18.006 Postcondition 4) — `E-SHD-002: shard archival move failed for artifact_stem "<artifact>" (shard "<sealed-path>") — retention/compaction aborted, no shard-index change applied: <io-error>` (BC-1.18.007 Postcondition 2); **(c)** `ShardRetentionError::ArchivalIndexEntryVanished` (NEW, this PR, commit `4febaaa7`, replacing an unreachable `.expect()` with a typed fail-loud guard per CLAUDE.md's production-grade default) — an invariant guard inside `archive_overflow_shards`: a `seq` collected from `index.shards` at the start of the archival pass was no longer present in `index.shards` when the pass looked it back up by position; provably unreachable today (the loop only mutates matched entries in place, never removes them, between collection and lookup) but fails loud via a typed error rather than panicking, in case a future refactor breaks that invariant — `E-SHD-002: shard archival invariant violated for artifact_stem "<artifact>" — shard seq <seq> was collected from index.shards at the start of this archival pass but is no longer present in index.shards`. **Nesting note:** when `archive_overflow_shards` is invoked from `run_mechanism_a_backfill_split`'s Postcondition 4 composition step (BC-1.18.008) rather than from BC-1.18.007's own standalone retention check, ANY of these three forms is wrapped as `MechanismABackfillError::Io` and surfaces as a NESTED double-code message — see `E-SHD-003`'s row below for the exact composed text. CORRECTED (F-C3-P9-002) from the prior single-form cell, which (i) omitted the `E-SHD-002:` code prefix, (ii) used bare `for <artifact>:` instead of the shipped `for artifact_stem "<artifact>":` quoting convention, (iii) mislabeled the `#[source]` field `<parse-error>` when the shipped field type is a plain `io::Error` (this table's own convention for a `#[source] io::Error` field elsewhere is `<io-error>`, e.g. `E-SHD-001`/`E-SHD-006`/`E-SHD-007`/`E-SHD-008`), and (iv) documented only `IndexUnavailable`, leaving `ArchivalMoveFailed`'s real emission entirely undocumented. |
| E-SHD-003 | Shard management errors | broken | `HookResult::Error` | TWO distinct real emissions under this ONE code (this amendment, F-C3-P9-002, S-25.02 F4 cluster-3 fresh LOCAL adversarial review, MEDIUM): **(a)** `MechanismABackfillError::ContentPreservationFailed` — Postcondition 6(a)/6(b)'s mandatory content-preservation/record-integrity verification gate failed (the staged partitions, concatenated in order, do not reproduce the original monolithic file byte-for-byte, or a structural record was found duplicated or dropped across the staged partitions; `detail` names which check failed and how) — `E-SHD-003: backfill-split content-preservation verification failed for artifact_stem "<artifact>": <detail>` (BC-1.18.008 EC-004, Postcondition 6); **(b)** `MechanismABackfillError::Io` — a genuine I/O failure while staging the split's shard files and index (Postcondition 5's stage-then-verify-then-atomically-replace sequence); the original monolithic file is left untouched and the operation is safely re-runnable from scratch — `E-SHD-003: backfill-split I/O failure for artifact_stem "<artifact>": <io-error>` (BC-1.18.008 Postcondition 5, EC-003). CORRECTED from the prior single-form cell, which (i) omitted the `E-SHD-003:` code prefix, (ii) used bare `for <artifact>:` instead of the shipped `for artifact_stem "<artifact>":` quoting convention, and (iii) documented only `ContentPreservationFailed`, leaving `Io`'s real emission entirely undocumented. **Nested double-code composition (this amendment, F-C3-P9-005):** when form (b) (`Io`)'s underlying failure originates from `run_mechanism_a_backfill_split`'s Postcondition 4 retention-composition step (`archive_overflow_shards`, wrapped via `io::Error::other(source.to_string())`), the rendered `{source}` text is itself a COMPLETE, already-formatted `E-SHD-002` message (any of that code's three forms — see `E-SHD-002`'s row above) — producing a nested double-code string of the exact shape `E-SHD-003: backfill-split I/O failure for artifact_stem "<artifact>": E-SHD-002: shard archival move failed for artifact_stem "<artifact>" (shard "<sealed-path>") — retention/compaction aborted, no shard-index change applied: <io-error>`. This is intentional, not a formatting defect: `MechanismABackfillError::Io`'s `#[source]` field is a plain `io::Error`, and `ShardRetentionError`'s own `Display` text (which already carries its own `E-SHD-002:` prefix) is what gets stringified into it — operators and log parsers should expect an `E-SHD-002:`-prefixed substring nested inside an `E-SHD-003:` message specifically for this ONE call path (Postcondition 4 composition), never for `ContentPreservationFailed` (form (a), which never wraps a `ShardRetentionError`) or for a genuine standalone I/O failure elsewhere in the staging sequence (which nests a bare OS `io::Error` string, no second code). |
| E-SHD-004 | Shard management errors | broken | `HookResult::Error` | `E-SHD-004: rotate_changelog invocation failed for "<artifact>": <io-error>` (BC-1.18.009 EC-003; CORRECTED from `rotate_changelog invocation failed for <artifact>: <io-error>` — was missing the `E-SHD-004:` code prefix and `"<artifact>"` quoting matching every other implemented E-SHD row; F-C4-ADV-001. Cluster-4 hardening first live emission, S-25.02) |
| E-SHD-005 | Shard management errors | broken | `HookResult::Error` / migration abort | `B2 BC-INDEX migration verification failed (content-preservation or independent-census mismatch): <mismatch-detail>; original body left untouched` (BC-1.18.011 Postconditions 2/4, EC-001/EC-004) |
| E-SHD-006 | Shard management errors | broken (self-healing) | `HookResult::Error` at the CRASHING dispatch (the roll step (c) failure surfaces `HookResult::Error` naming `E-SHD-006` — never `HookResult::Block`; a `Block` here would falsely imply the shard is now empty and safe to retry against, per Invariant 1) / no `HookResult` of its own on the SUBSEQUENT recovery dispatch (self-heal is silent: `run_self_heal_if_plausible` runs before that dispatch's own trigger is evaluated, resumes from the truncate step, and — on success — falls through to normal, non-Error trigger handling; a self-heal I/O failure surfaces its own fresh `HookResult::Error`) | `E-SHD-006: canonical-truncate failed after sealed-shard publish succeeded for artifact_stem "<artifact>" (sealed shard "<sealed-path>" already durable) — resume-from-truncate self-heal required on next dispatch: <io-error>` (CORRECTED, S-25.02 cluster-2 LOCAL adversary pass-8, F-C2-P8-003, MINOR: this column previously documented a `(seq=<N>)` token the shipped `Display` never emits — the actual `TruncateFailedAfterSeal` message names the sealed shard by its `<sealed-path>` filename, e.g. `decision-log.0001.md`, not a bare `seq` number; BC-1.18.006 Postcondition 1 step (c) failure after step (b) succeeded; NOT a data-loss condition — the next dispatch attempt self-heals by resuming from the truncate step alone) |
| E-SHD-007 | Shard management errors | broken (self-healing) | `HookResult::Error` at the CRASHING dispatch (the roll step (d) failure surfaces `HookResult::Error` naming `E-SHD-007` — never `HookResult::Block`; a `Block` here would falsely imply the shard is now empty and safe to retry against, per Invariant 1) / no `HookResult` of its own on the SUBSEQUENT recovery dispatch (self-heal is silent: `run_self_heal_if_plausible` runs before that dispatch's own trigger is evaluated, reconciles the index, and — on success — falls through to normal, non-Error trigger handling; a self-heal I/O failure surfaces its own fresh `HookResult::Error`) | `E-SHD-007: shard-index publish failed after canonical-truncate succeeded for artifact_stem "<artifact>" (sealed shard "<sealed-path>" already durable, canonical already empty) — index reconciliation required on next dispatch: <io-error>` (CORRECTED, S-25.02 cluster-2 LOCAL adversary pass-8, F-C2-P8-003, MINOR: this column previously documented a `(seq=<N>)` token the shipped `Display` never emits — the actual `IndexPublishFailedAfterTruncate` message names the sealed shard by its `<sealed-path>` filename, not a bare `seq` number; BC-1.18.006 Postcondition 1 step (d) failure after step (c) succeeded; a discoverability-metadata gap only — the next dispatch attempt self-heals by scanning for un-indexed sealed shards and appending the missing entry) |
| E-SHD-008 | Shard management errors | broken | `HookResult::Error` | `E-SHD-008: Write-arm backstop stat() failed for artifact_stem "<artifact>" at '<path>' — cannot confirm whether the canonical file is a crash-orphaned, over-cap shard; refusing to let this Write proceed until the underlying I/O condition is resolved: <io-error>` (CORRECTED, S-25.02 cluster-2 LOCAL adversary pass-9, F-C2-P9-002, MINOR: this column previously documented an abbreviated, non-matching `backstop probe stat failure for <artifact>: <io-error>` wording the shipped `Display` never emits — the actual `BackstopProbeFailed` message is reproduced verbatim above with placeholders. BC-1.18.006 EC-019, Postcondition 7 catch point (ii); the dedicated `Write`-arm backstop `stat()` of the canonical file failed with a non-`NotFound` error — fails LOUD, never open, since a dereferencing `stat()` failure and a non-dereferencing `write_atomic` rename onto the same path can fail asymmetrically (e.g. a final-component symlink loop); the `Write` is never applied) |
| E-SHD-009 | Shard management errors | broken | `HookResult::Error` | `E-SHD-009: refusing to overwrite an already-sealed shard at '<path>' for artifact_stem "<artifact>" — sealed shards are write-once/immutable; this seq already has durable content on disk` (CORRECTED, S-25.02 cluster-2 LOCAL adversary pass-9, F-C2-P9-002, MINOR: this column previously documented a non-matching `sealed-shard immutability violation: refusing to overwrite an existing seal at <path>` wording the shipped `Display` never emits — the actual `SealedShardAlreadyExists` message is reproduced verbatim above with placeholders. BC-1.18.006 EC-024, Postcondition 8; `publish_sealed_shard` is write-once — before every seal-publish `rename(temp, <stem>.<seq:04>.md)`, it MUST verify the destination does not already exist; found existing, it refuses the rename and fails LOUD rather than silently overwriting durable, append-only-sealed history. Defense-in-depth for the F-C2-P4-001 data-loss counterexample — an unreconciled index colliding `next_seal_seq` against an already-occupied `seq` — closed at the root by BC-1.18.006's corrected Invariant 10/Postcondition 7 catch point (i) self-heal-first sequencing; this code is the second, independent fail-safe layer, converting any residual collision into a loud error instead of a silent overwrite. Neither the seal, the truncate, nor the index publish are applied for the refused attempt) |
| E-SHD-010 | Shard management errors | broken | `HookResult::Error` | `E-SHD-010: refusing to read or stat '<path>' for artifact_stem "<artifact>" — the canonical path is a symlink, not a regular file; refusing to dereference it (symlink-exfiltration hardening, BC-1.18.006 FIX-MED-2)` (NEW, PR #824 fresh-eyes review Finding #7, MINOR — this row was previously missing entirely for the already-shipped `ShardRollError::CanonicalPathIsSymlink` variant and its `reject_canonical_symlink` guard, `crates/factory-dispatcher/src/shard_manager.rs`; Message Format verified verbatim against the shipped `Display` impl. No `#[source] io::Error`, mirroring `E-SHD-009`'s convention: this is a detected INVARIANT violation — the path IS a symlink, confirmed via non-dereferencing `symlink_metadata`/lstat, never a live `stat()`/`read()` through it — not an underlying I/O failure. Hardens against a symlink-exfiltration channel: if a canonical artifact path were replaced by a symlink to a sensitive file elsewhere, silently reading/statting through it would let the symlink target's bytes be durably sealed into an ordinary, committable regular file. **This code's own dedicated Verification Property is DEFERRED to S-25.02's own Phase F6 (Targeted Hardening)** — the identical concrete, scheduled anchor this cluster's EC-013..EC-025 precedent already uses for every other VP-allocation deferral (see BC-1.18.006's Postcondition 7/8 "OWED §4.4 F6-owed list" and VP-119's changelog), satisfying CLAUDE.md Canonical Principle Rule 3 in full: explicit human direction (this burst's converge-to-PR authorization), a concrete future dependency (architect/formal-verifier fault-injection coverage of the symlink guard, mirroring the EC-025 O_EXCL-primitive precedent), and a named, non-"later" anchor (S-25.02 Phase F6 targeted-hardening). The separate Edit/MultiEdit-arm guard-coverage gap this same PR review's Finding #3 identifies is NOT this deferral's subject — that is a code+test defect routed to implementer/test-writer in-scope, not deferred) |
| E-SHD-011 | Shard management errors | broken | `HookResult::Error` | TWO distinct real emissions under this ONE code (form (b)'s citation CORRECTED this amendment, F-C3-P9-004, S-25.02 cluster-3 fresh-eyes PR #831 reviewer finding, BLOCKING — the prior citation below was FABRICATED: BC-1.18.008 v1.7/v1.8 Invariant 3 had NO sub-clause "(c)" addressing a missing-manifest state at all; its three-way disposition list (a)/(b)/(c) covered only SAFE/DANGEROUS/AMBIGUOUS determinations that already PRESUPPOSE a Manifest exists to compare against): **(a)** the top-level three-way `(length, hash)` mismatch — `E-SHD-011: backfill recovery-confirmation ambiguous for artifact_stem "<artifact>" — canonical file bytes (<len> bytes, sha256 <hash>) match NEITHER the pre-split original (<original_bytes> bytes, sha256 <original_sha256>) NOR the intended final partition (<final_bytes> bytes, sha256 <final_sha256>) recorded in the Backfill Recovery Manifest — refusing to guess; canonical file left untouched pending operator investigation`; **(b)** `heal_or_confirm_already_migrated`'s own independent re-read of the shard-index finds no `backfill_manifest` field populated, DESPITE `mechanism_a_backfill_already_migrated`'s upfront check having already confirmed one present — a genuine TOCTOU race (the index was concurrently rewritten between the two reads) or on-disk corruption of the `backfill_manifest` field specifically; a **narrower** residual case than previously documented — `E-SHD-011: backfill recovery-confirmation ambiguous for artifact_stem "<artifact>" — the published shard-index has no Backfill Recovery Manifest ([backfill_manifest]) to compare the canonical file's current bytes against — refusing to guess; canonical file left untouched pending operator investigation`. Both forms share the same code, severity, `HookResult::Error` outcome, and "refuse to guess / canonical file left untouched" disposition — form (b) is a precondition failure for the SAME recovery-confirmation determination form (a) performs, not a distinct error class. **REAL citation for form (b), replacing the fabricated one (BC-1.18.008 v1.9, this amendment):** Invariant 3's **Residual `MissingBackfillManifest` disposition** clause (F-C3-P9-004) — added specifically because BC-1.18.008 v1.9 ALSO redefines "already migrated" to key on `backfill_manifest.is_some()` rather than bare shard-index-file existence (Postcondition 3's Composability clause), which means the FAR MORE COMMON manifest-less-index case — an ordinary BC-1.18.006 roll sealed this artifact BEFORE the mandatory one-time backfill ever ran (the "roll-before-backfill" ordering, EXPECTED in practice) — no longer reaches `heal_or_confirm_already_migrated`/form (b) AT ALL; it is correctly routed to a fresh backfill run instead (BC-1.18.008 EC-015). Message-Format cell text CORRECTED at F-C3-P9-001 against the shipped `MechanismABackfillError::MissingBackfillManifest` `Display` (unchanged by this amendment — only the citation and legitimate-firing-condition narrative changed). (Form (a) NEW, S-25.02 F4 cluster-3 CROSS-VENDOR (OpenAI Codex) adversarial review, F-C3-P6-001, HIGH — BC-1.18.008 v1.6 Postcondition 5 Recovery-Confirmation Rule, Invariant 3, EC-010. Closes a data-loss gap: the prior structural byte-prefix recovery heuristic false-positived on legitimate repeated-prefix content — e.g. `session-checkpoints.md` content where a sealed shard's bytes recur as the leading prefix of the artifact's own legitimately-migrated final partition — and silently overwrote the canonical file. The corrected mechanism compares the canonical file's exact whole-file `(length, SHA-256)` against two values recorded once, durably, in the shard-index's `[backfill_manifest]` table at split time; a match to neither is this error, and is the ONLY disposition under which recovery halts rather than resolving SAFE (no-op) or DANGEROUS (completes the interrupted truncate). Both forms remain NOT YET IMPLEMENTED in `shard_manager.rs` — `mechanism_a_backfill_already_migrated`/`heal_or_confirm_already_migrated` require restructuring per BC-1.18.008 v1.6's original changelog AND v1.9's schema-location/idempotency-basis correction (this amendment); this taxonomy row defines the contract those functions must implement, including form (b)'s narrowed, post-v1.9 firing condition.) |
| E-SHD-012 | Shard management errors | broken | `HookResult::Error` | **CORRECTED (this amendment, F-C3-P8-001, S-25.02 F4 cluster-3 fresh LOCAL adversarial review, MEDIUM): the shipped `MechanismABackfillError::SliceVerificationFailed` `Display` emits a `{detail}`-placeholder form, not the fixed structured wording this row previously documented — the prior wording was, per this cluster's own recurring "Message Format ≠ emitted Display" drift pattern (corrected 4 times already for other `E-SHD-NNN` codes), out of sync with the actual shipped text.** Actual emitted format: `E-SHD-012: backfill recovery heal slice-verification failed for artifact_stem "<artifact>": <detail>` — documented, per this table's existing convention for other `{detail}`-bearing variants (e.g. `E-SHD-002`'s `<parse-error>`, `E-SHD-005`'s `<mismatch-detail>`), with the FIVE distinct `<detail>` shapes the shipped code populates it with: (1) **manifest inconsistency (checked_sub underflow)** — `final_bytes` exceeds `original_bytes` in the Manifest itself, so the offset subtraction `original_bytes - final_bytes` cannot be performed, e.g. `manifest inconsistency: final_bytes (<final_bytes>) exceeds original_bytes (<original_bytes>)`; (2) **offset arithmetic overflow** — computing the slice offset or offset-plus-length overflows `usize`, e.g. `offset computation overflowed usize`; (3) **offset exceeds canonical length** — the Manifest-derived offset exceeds the canonical file's actual on-disk length, so the slice cannot be taken, e.g. `offset <offset> exceeds canonical file length <len>`; (4) **pre-write slice mismatch (EC-011)** — the slice taken at the Manifest-authoritative offset fails `sliced.len() == final_bytes AND sha256(sliced) == final_sha256` BEFORE any write is issued, e.g. `slice (<sliced_len> bytes, sha256 <sliced_sha256>) does not match manifest final partition (<final_bytes> bytes, sha256 <final_sha256>) — refusing to write an unverified slice`; (5) **post-hoc read-back mismatch** — the heal's `write_atomic` call has ALREADY completed, but the mandatory fresh disk read-back (Postcondition 6(c)'s F-C3-P7-001 extension) does not match `(final_bytes, final_sha256)`, e.g. `post-write read-back (<len> bytes, sha256 <hash>) does not match manifest final partition (<final_bytes> bytes, sha256 <final_sha256>) — canonical file already written; heal write may be corrupted, requires operator investigation from git history/backup`. Cases (1)-(4) all guarantee "canonical file left untouched" (no write has been issued yet); case (5) does NOT — the destructive write already happened, and the message's own detail text says so, distinguishing it operationally from (1)-(4) despite sharing the same error code. (NEW, S-25.02 F4 cluster-3 FRESH LOCAL adversarial review, F-C3-P7-001, HIGH — BC-1.18.008 v1.7 Postcondition 5 Manifest-Authoritative Slice-and-Verify Rule, Postcondition 6(c)'s F-C3-P7-001 extension, Invariant 3, EC-011. Distinct from `E-SHD-011`: this fires ONE LEVEL DEEPER, after the top-level `(length, hash)` recovery-confirmation check has ALREADY confirmed the DANGEROUS window. Closes the gap the v1.6 DANGEROUS-heal text left open: v1.6 said heal "writes the manifest's own recorded `final_bytes` content ... never re-derived by slicing," which is unimplementable against the Manifest's own length-plus-hash-only schema; a prior implementation pass instead derived the slice offset by summing the shard-index's per-shard `bytes_at_seal` fields — a source independent of, and separately corruptible from, the Manifest — and wrote the result with NO verification at all. `E-SHD-012` is the hard gate that makes such a mis-heal impossible.) |
| E-SHD-013 | Shard management errors | broken | `HookResult::Error` | `E-SHD-013: post-hoc canonical-file verification failed after happy-path final-partition write for artifact_stem "<artifact>" — fresh disk read-back (<len> bytes, sha256 <hash>) does NOT match the Backfill Recovery Manifest's recorded final partition (<final_bytes> bytes, sha256 <final_sha256>) — the destructive write already completed; original monolithic content is no longer recoverable from the canonical file itself; halting for operator investigation from git history/backup` (NEW, S-25.02 F4 cluster-3 fresh LOCAL adversarial review, F-C3-P8-002, MEDIUM — BC-1.18.008 v1.8 Postcondition 6(c)'s new happy-path-canonical-write extension, Invariant 5, EC-013. Distinct from `E-SHD-012`: this fires on the ORDINARY, non-recovery completion of Postcondition 5 step (ii) — the FIRST, uninterrupted run of the migration's own final destructive write — never during a crash-recovery heal; it exists to close the asymmetry where the heal write (F-C3-P7-001) received a mandatory post-hoc disk read-back but the far-more-frequently-executed happy-path write that the heal exists to RECOVER FROM did not, leaving a silently-truncated or corrupted happy-path write undetectable at migration time. The oracle is the SAME Backfill Recovery Manifest `(final_bytes, final_sha256)` pair already durably published in Postcondition 5 step (i), before step (ii) ever runs — no new value is computed for this check. Not yet implemented in `shard_manager.rs` as of this row's creation — `run_mechanism_a_backfill_split`'s happy-path completion path requires a new post-hoc read-back step and a new `MechanismABackfillError` variant per BC-1.18.008 v1.8's changelog; this taxonomy row defines the contract that addition must implement) |
| E-SHD-014 | Shard management errors | broken | `HookResult::Error` | `E-SHD-014: rotate_changelog_at returned Ok(mutated=false) after item-count trigger fired for "<artifact>": trigger (serde read_changelog_item_count) counts >= N but rotate_changelog_at line-scan (parse_frontmatter) found total <= keep_recent — counter-method divergence; manual inspection of <path> required` (BC-1.18.009 EC-008, Invariant Inv-5. Counter-divergence guard: the B1 handler's item-count trigger uses `read_changelog_item_count` via serde deserialization of the YAML array; `rotate_changelog_at`'s own no-op guard uses `parse_frontmatter` line-scan — on canonical `  - date:` format the two count the same items, but the coupling is unguarded. When `Ok(report)` carries `report.mutated == false` after the trigger fired, returning `Block` would produce an infinite block+retry self-DoS on `BC-INDEX.md` for the session duration; this `Error` intercepts it. Distinct from `E-SHD-004` (`rotate_changelog_at` returned `Err` — I/O or validation failure): `E-SHD-014` is an `Ok` arm, not an `Err` arm. Implemented in `shard_manager.rs` as of S-25.02 cluster-4 hardening (status updated v1.16 — the "NOT yet implemented as of this row's creation" qualifier from v1.15 is now retired; Message Format unchanged, already matched shipped Display). **Error code note:** the hardening design doc cited `E-SHD-008` as the "next free slot" — at the time of that document's production, `E-SHD-008` through `E-SHD-013` were already allocated in this taxonomy (E-SHD-008 = `BackstopProbeFailed` BC-1.18.006 EC-019, allocated at v1.3; E-SHD-009..E-SHD-013 allocated at v1.4..v1.12). Actual next free slot: `E-SHD-014`.) |
| E-SHD-015 | Shard management errors | broken | `HookResult::Error` | `E-SHD-015: cannot resolve archive path — target_path '<path>' has no parent directory component` (BC-1.18.009 SEC-002 fix; introduced c74c3014, S-25.02 cluster-4. The B1 handler computes the evergreen archive sibling path as `<target_path.parent()>/<artifact_stem>-changelog-archive.md`; when `target_path.parent()` returns `None` — a target path with no directory component, e.g. a bare filename with no leading directory — the archive sibling path cannot be computed without silently resolving into the process's CWD via the prior `unwrap_or_else(\|\| Path::new("."))` fallback. Rather than silently write the archive to an unexpected location, the gate fails loud with this error. In practice `target_path` is always an absolute path derived from the dispatcher's shard config entry, so this error fires only in misconfigured or test-harness environments. Distinct from `E-SHD-004` (rotate call I/O failure) and `E-SHD-014` (counter-divergence guard): `E-SHD-015` fires BEFORE the rotate call is attempted, during archive path construction.) |

All dispatcher-level errors except E-CAP and E-PLG exit 0 (non-blocking per NFR-REL-001).
`E-SHD-NNN` errors are native-gate `HookResult::Error` outcomes (a PreToolUse hook result, not a
process exit code) — dispatcher exit-code semantics do not apply to this category, per BC-1.18.006
through BC-1.18.011's own Postcondition/Edge-Case tables.

## Severity Definitions

| Severity | Meaning | Exit Code / HookResult Impact |
|----------|---------|-------------------------------|
| broken | Cannot continue | Non-zero exit, OR (for E-SHD) `HookResult::Error` — the triggering write is blocked and no partial state is left authoritative |
| blocked | Explicitly denied by policy | Exit 2 |
| degraded | Partial result possible | Zero exit with warnings, non-blocking |
| cosmetic | Formatting/display issue | Zero exit |

## Changelog

| Version | Date | Author | Change |
|---------|------|--------|--------|
| 1.17 | 2026-09-11 | product-owner | Added **`E-SHD-015`** (BLK-C2-2, S-25.02 cluster-4 PR cycle-2 blocker). `E-SHD-015` is emitted when the B1 handler's archive path construction fails because `target_path.parent()` returns `None` — i.e. `target_path` has no parent directory component (SEC-002 fix, introduced c74c3014). Closes the missing-taxonomy-entry gap: the code was shipped in cluster-4 but no taxonomy row existed, leaving operators without a documentation anchor for the error. Message Format cell verified verbatim against the shipped `format!("E-SHD-015: ...")` in `crates/factory-dispatcher/src/shard_manager.rs`. **input-hash recompute owed to state-manager** (this file's own content changed v1.16→v1.17). |
| 1.16 | 2026-09-11 | product-owner | **Comprehensive E-SHD sweep** (adv re-cascade pass-1 F-C4-ADV-001, MEDIUM): re-diffed every E-SHD-NNN Message Format cell against the shipped `format!("E-SHD-NNN: ...")` Display string in `crates/factory-dispatcher/src/shard_manager.rs`. **Implemented codes verified:** `E-SHD-001` (SealWriteFailed) MATCH; `E-SHD-006` (TruncateFailedAfterSeal) MATCH; `E-SHD-007` (IndexPublishFailedAfterTruncate) MATCH; `E-SHD-008` (BackstopProbeFailed) MATCH; `E-SHD-009` (SealedShardAlreadyExists) MATCH; `E-SHD-010` (CanonicalPathIsSymlink) MATCH. **E-SHD-002/003/005/011/012/013:** not yet emitted in any Rust source (grep confirmed zero hits across all crates) — no false implementation attestation in live cells; rows unchanged. **`E-SHD-004` DRIFT FIXED (F-C4-ADV-001):** prior cell read `rotate_changelog invocation failed for <artifact>: <io-error>` — missing the `E-SHD-004:` code prefix and the `"<artifact>"` quoting that the B1 handler emits; CORRECTED to `E-SHD-004: rotate_changelog invocation failed for "<artifact>": <io-error>`. E-SHD-004 was latent before cluster-4; its emission first goes live with S-25.02 cluster-4 delivery. **`E-SHD-014` status note updated:** removed "NOT yet implemented in shard_manager.rs as of this row's creation" qualifier — E-SHD-014 IS implemented as of cluster-4 hardening (S-25.02); Message Format cell unchanged (already matched shipped Display per adversary). **input-hash recompute owed to state-manager** (this file's own content changed v1.15→v1.16; BC-1.18.009.md input content also changed v1.6→v1.7 in the same burst). |
| 1.15 | 2026-09-11 | product-owner | Cluster-4 hardening amendment (S-25.02 mechanism B1, observation A per `s2502-cluster4-hardening-design.md`). Added **`E-SHD-014`** (counter-method divergence guard: `rotate_changelog_at` returned `Ok(mutated=false)` after item-count trigger fired — the B1 handler's serde `read_changelog_item_count` trigger path and `rotate_changelog_at`'s `parse_frontmatter` line-scan path counted differently; `HookResult::Error(E-SHD-014)` structurally prevents the infinite block+retry self-DoS on `BC-INDEX.md`; BC-1.18.009 EC-008, Invariant Inv-5). Distinct from `E-SHD-004` (Err arm I/O failure): `E-SHD-014` is an `Ok` arm counter-divergence signal. Not yet implemented in `shard_manager.rs` as of this row's creation. **Error code correction from design doc:** the hardening design doc (`s2502-cluster4-hardening-design.md`, produced 2026-09-11) cited `E-SHD-008` as the "next free slot" — that assessment was stale; `E-SHD-008` (`BackstopProbeFailed`, BC-1.18.006 EC-019) was already allocated at taxonomy v1.3, and codes run through `E-SHD-013` (`CanonicalWriteVerificationFailed`) at taxonomy v1.12. Actual next free slot: **`E-SHD-014`**. `inputs:` list unchanged (BC-1.18.009.md already listed; no other input files changed); **input-hash recompute owed to state-manager** (this file's own content changed v1.14→v1.15; BC-1.18.009.md input content also changed v1.5→v1.6 in the same burst). |
| 1.14 | 2026-09-10 | product-owner | Taxonomy corrections accompanying BC-1.18.008 v1.8→v1.9 (S-25.02 cluster-3, fresh-eyes PR #831 reviewer finding BLOCKING-1). **`E-SHD-011` form (b) citation CORRECTED (F-C3-P9-004, BLOCKING):** the prior row cited "BC-1.18.008 v1.7 Invariant 3(c)'s explicit sanction of reusing `E-SHD-011` for the missing-manifest case" as form (b)'s spec authority — direct inspection of BC-1.18.008 v1.7/v1.8 confirmed Invariant 3 has NO sub-clause "(c)" addressing a missing-manifest state at all; its three-way (a)/(b)/(c) disposition list covers only SAFE/DANGEROUS/AMBIGUOUS determinations that already presuppose a Manifest exists. CORRECTED to cite BC-1.18.008 v1.9's new **Residual `MissingBackfillManifest` disposition** clause in Invariant 3, and NARROWED form (b)'s documented firing condition: BC-1.18.008 v1.9 also redefines "already migrated" to key on `backfill_manifest.is_some()` (not bare shard-index-file existence), so the previously-documented example ("a shard-index published by a pre-v1.6 code path... before the Backfill Recovery Manifest existed") — which is actually the far-more-common roll-before-backfill ordering — no longer reaches form (b) at all; it is now correctly routed to a fresh backfill run (BC-1.18.008 EC-015) instead. Form (b) post-v1.9 legitimately fires ONLY on a genuine TOCTOU race between the idempotency check's load and the heal's own independent re-read, or on-disk corruption of the `backfill_manifest` field specifically. Message-Format text itself UNCHANGED (still matches the shipped `Display` per F-C3-P9-001's prior verbatim correction) — only the citation and the legitimate-firing-condition narrative changed. **`E-SHD-002` extended to document a THIRD real emission (F-C3-P9-005, MINOR):** this same PR (commit `4febaaa7`) added `ShardRetentionError::ArchivalIndexEntryVanished`, a new fail-loud invariant guard inside `archive_overflow_shards` replacing an unreachable `.expect()` per CLAUDE.md's production-grade default — documented as form (c), bringing this table's own exhaustiveness attestation (v1.13: "13 codes, 17 real emissions") to 18 real emissions across the same 13 codes. Also added a **nesting note** to both `E-SHD-002`'s and `E-SHD-003`'s rows: when `archive_overflow_shards` fails during `run_mechanism_a_backfill_split`'s Postcondition 4 retention-composition step, the failure is wrapped as `MechanismABackfillError::Io` and surfaces as a NESTED double-code message of the shape `E-SHD-003: backfill-split I/O failure for artifact_stem "<artifact>": E-SHD-002: <the wrapped ShardRetentionError's own already-E-SHD-002-prefixed Display text>` — intentional (the wrapped `#[source]` field is a plain `io::Error` populated from `ShardRetentionError`'s own `Display`), not a formatting defect, and previously undocumented. `inputs:` list unchanged (still cites `BC-1.18.008.md`, now at v1.9 content); **input-hash recompute owed to state-manager** (this file's own content changed v1.13→v1.14; `BC-1.18.008.md` input content also changed v1.8→v1.9 in the same burst). |
| 1.13 | 2026-09-10 | product-owner | **Complete E-SHD-NNN sweep** (fresh local adversarial pass on S-25.02 F4 cluster-3, 6th+ recurrence of the "Message Format cell ≠ shipped Display" drift class; human-directed to stop fixing one code at a time and instead mechanically re-diff EVERY emitted `E-SHD-NNN` Display against this table in a single pass). Grepped every `#[error("E-SHD-...")]` Display in `crates/factory-dispatcher/src/shard_manager.rs` (both `ShardRollError` and `MechanismABackfillError`, plus the previously-unswept `ShardRetentionError`) and diffed each against its Message Format cell verbatim. **Full diff table (13 codes, 17 real emissions):** `E-SHD-001` (`ShardRollError::SealWriteFailed`) MATCH, no change. `E-SHD-002` (`ShardRetentionError`, TWO emissions) DRIFT, FIXED — cell previously omitted the `E-SHD-002:` prefix, used bare `for <artifact>:` instead of `for artifact_stem "<artifact>":`, mislabeled the `#[source] io::Error` field `<parse-error>`, and documented only `IndexUnavailable`; `ArchivalMoveFailed`'s real emission was entirely undocumented — corrected to document both forms verbatim, mirroring the format already used for `E-SHD-011`'s two forms. `E-SHD-003` (`MechanismABackfillError`, TWO emissions) DRIFT, FIXED (F-C3-P9-002) — cell omitted the `E-SHD-003:` prefix, used bare `for <artifact>:` quoting, and documented only `ContentPreservationFailed`; the `Io` variant's real emission was entirely undocumented — corrected to document both forms verbatim. `E-SHD-004`/`E-SHD-005` NOT YET IMPLEMENTED anywhere in the workspace (`grep -rn "E-SHD-004\|E-SHD-005" --include="*.rs" crates/` returns zero hits) — no Display exists to diff against; rows unchanged, no drift claim made. `E-SHD-006` (`TruncateFailedAfterSeal`) MATCH, no change. `E-SHD-007` (`IndexPublishFailedAfterTruncate`) MATCH, no change. `E-SHD-008` (`BackstopProbeFailed`) MATCH, no change. `E-SHD-009` (`SealedShardAlreadyExists`) MATCH, no change. `E-SHD-010` (`CanonicalPathIsSymlink`) MATCH, no change. `E-SHD-011` (`MechanismABackfillError`, TWO emissions) form (a) `AmbiguousRecoveryState` MATCH; form (b) `MissingBackfillManifest` DRIFT, FIXED (F-C3-P9-001) — cell read "backfill recovery cannot proceed for" vs shipped "backfill recovery-confirmation ambiguous for", omitted the shipped "([backfill_manifest]) to compare the canonical file's current bytes against" clause, and read "refusing to guess whether ... SAFE, DANGEROUS, or AMBIGUOUS without it;" vs the shipped bare "refusing to guess;" — corrected to the shipped text verbatim. `E-SHD-012` (`SliceVerificationFailed`) MATCH, no change (re-verified per this burst's instruction to re-check the just-added v1.12 rows; the `{detail}`-placeholder form documented in v1.12 already matches the shipped Display verbatim). `E-SHD-013` (`CanonicalWriteVerificationFailed`) MATCH, no change (re-verified; already matches shipped Display verbatim). **All-rows-source-verified attestation:** every `E-SHD-NNN` row in this table (`E-SHD-001` through `E-SHD-013`) has now been individually re-diffed against its shipped Rust source in this single burst, covering `ShardRollError`, `MechanismABackfillError`, AND `ShardRetentionError` — the prior v1.8 changelog's "all nine rows source-verified" attestation covered only `ShardRollError`; this is the first attestation covering all three enums in one pass. **No BC change required**: the shipped code Displays were already correct in every case; only this taxonomy's Message Format cells were stale. BC-1.18.008 v1.8 already governs the semantics for the `MechanismABackfillError` codes and needs no amendment. **Process-gap flagged for state-manager** (not created here, per this burst's own instruction not to author the follow-up story): this drift class has now recurred 6+ times (v1.2, v1.6, v1.7, v1.8, v1.9, v1.10/1.11 partial, and this v1.13 sweep) because no gate mechanically re-diffs the FULL emitted-Display set against this table — every prior fix reconciled only the one code the triggering finding named, letting a different code drift by the next pass. Recommend a lint hook / policy extension: `grep -oE '"E-SHD-[0-9]+:[^"]*"' crates/factory-dispatcher/src/shard_manager.rs` (and any future E-SHD-emitting file) diffed against this table's Message Format column, run in CI or as a pre-commit gate whenever either side changes — attach to a concrete future story, not "later." `inputs:` list unchanged (no BC file content changed this burst — only this file's own body); **input-hash recompute owed to state-manager** (this file's own content changed v1.12→v1.13). |
| 1.12 | 2026-09-10 | product-owner | Taxonomy pass closing two findings from a fresh LOCAL adversarial review of S-25.02 F4 cluster-3, folded into a single burst per the review's own routing (F-C3-P8-001, MEDIUM, this file; F-C3-P8-002, MEDIUM, sibling BC-1.18.008 v1.7→v1.8 amendment). **F-C3-P8-001 — `E-SHD-012` Message Format ≠ shipped `Display`, RESOLVED.** This row previously documented a fixed structured Message Format; the shipped `MechanismABackfillError::SliceVerificationFailed` `Display` actually emits `E-SHD-012: backfill recovery heal slice-verification failed for artifact_stem "{artifact_stem}": {detail}` — a `{detail}`-placeholder form covering FIVE distinct detail strings (manifest-inconsistency `checked_sub` underflow, `usize` offset-arithmetic overflow, offset-exceeds-canonical-length, the pre-write slice mismatch this row previously documented as if it were the only case, and a post-hoc read-back mismatch after the heal's own write already completed). CORRECTED the cell to the `{detail}`-placeholder form, documented the way this table's other `{detail}`-bearing variants already are, and enumerated all five detail shapes with representative wording, noting that cases (1)-(4) leave the canonical file untouched while case (5) does not (the write already happened by that point). **Also completed `E-SHD-011`** per the same adversarial pass's observation: `MechanismABackfillError::MissingBackfillManifest` emits a SECOND distinct message under this code (`... the published shard-index has no Backfill Recovery Manifest ...`) that the row previously documented only the three-way `(length, hash)` mismatch form for. BC-1.18.008 v1.7 Invariant 3(c) sanctions reusing `E-SHD-011` for this missing-manifest precondition failure (a precondition failure for the same recovery-confirmation determination, not a distinct determination) — not a code defect, so no new code was allocated; the row now documents BOTH real emissions as forms (a) and (b) of the one code. **F-C3-P8-002 — added `E-SHD-013` (NEW)** for BC-1.18.008 v1.8's new happy-path canonical-truncate-write post-hoc verification requirement (Postcondition 6(c) extension, Invariant 5, EC-013) — the migration's ORDINARY, non-recovery completion of its own final destructive write now receives the same Manifest-verified disk read-back as the heal write, and a mismatch fails loud with this new code rather than silently reporting success over corrupted content. Not yet implemented in `shard_manager.rs` as of this row's creation — see BC-1.18.008 v1.8's own changelog for full implementer scope. `inputs:` list unchanged (still cites `BC-1.18.008.md`, now at v1.8 content); **input-hash recompute owed to state-manager** (this file's own content changed v1.11→v1.12; `BC-1.18.008.md` input content also changed v1.7→v1.8 in the same burst). |
| 1.11 | 2026-09-10 | product-owner | Sibling-site sweep accompanying BC-1.18.008 v1.6→v1.7 (S-25.02 F4 cluster-3 FRESH LOCAL adversarial review, F-C3-P7-001, HIGH): added `E-SHD-012` (backfill heal slice-verification failed) for the NEW fail-loud disposition BC-1.18.008 v1.7's Manifest-Authoritative Slice-and-Verify Rule (Postcondition 5) introduces — a confirmed-DANGEROUS-window heal whose Manifest-derived slice fails to satisfy `sliced.len() == final_bytes AND sha256(sliced) == final_sha256` against the Backfill Recovery Manifest. Distinct from `E-SHD-011` (a top-level three-way `(length, hash)` mismatch against BOTH manifest pairs): `E-SHD-012` fires only after the top-level check has ALREADY confirmed DANGEROUS, when the Manifest-verified-slice step itself fails — closing the gap BC-1.18.008 v1.6's DANGEROUS-heal text left open (it described writing "the manifest's own recorded `final_bytes` content," which the Manifest's length-plus-hash-only schema cannot literally supply, and had no verification step at all before the prior implementation pass's write). Not yet implemented in `shard_manager.rs` as of this row (`heal_or_confirm_already_migrated`'s DANGEROUS-window branch requires restructuring — see BC-1.18.008 v1.7's own changelog for full implementer scope). `inputs:` list unchanged (still cites `BC-1.18.008.md`, now at v1.7 content); **input-hash recompute owed to state-manager** (BC-1.18.008.md input content changed v1.6→v1.7). |
| 1.10 | 2026-09-10 | product-owner | Sibling-site sweep accompanying BC-1.18.008 v1.5→v1.6 (S-25.02 F4 cluster-3 CROSS-VENDOR (OpenAI Codex) adversarial review, F-C3-P6-001, HIGH): added `E-SHD-011` (backfill recovery-confirmation ambiguous state) for the NEW fail-loud disposition BC-1.18.008 v1.6's Recovery-Confirmation Rule (Postcondition 5) introduces — a canonical file whose bytes match neither the Backfill Recovery Manifest's recorded original-content nor intended-final-content `(length, SHA-256)` pair. This is a net-new error code closing a gap this same fix identified: the prior recovery mechanism (a structural byte-prefix heuristic) had NO ambiguous-state disposition at all — it only ever chose SAFE or DANGEROUS, silently, which is precisely the defect BC-1.18.008 v1.6 corrects. Not yet implemented in `shard_manager.rs` as of this row (`mechanism_a_backfill_already_migrated`/`heal_or_confirm_already_migrated` require restructuring — see BC-1.18.008 v1.6's own changelog for full implementer scope). `inputs:` list unchanged (still cites `BC-1.18.008.md`, now at v1.6 content); **input-hash recompute owed to state-manager** (BC-1.18.008.md input content changed v1.5→v1.6). |
| 1.9 | 2026-09-08 | product-owner | Fix-burst amendment resolving S-25.02 cluster-2 PR #824 fresh-eyes review Finding #7 (MINOR, spec side; routed product-owner per the Agent Routing Table). Added the missing `E-SHD-010` catalog row for the already-shipped `ShardRollError::CanonicalPathIsSymlink` variant (`reject_canonical_symlink` guard, `crates/factory-dispatcher/src/shard_manager.rs`, BC-1.18.006 FIX-MED-2) — the code's `Display` text existed with no corresponding taxonomy entry until this burst. Message Format cell verified verbatim against the shipped `#[error(...)]` text. Attached the previously-missing deferral anchor for this code's own Verification Property: **S-25.02 Phase F6 (Targeted Hardening)** — the same concrete, scheduled anchor already used by this cluster's EC-013..EC-025 VP-allocation-deferral precedent (BC-1.18.006 Postcondition 7/8 "OWED §4.4 F6-owed list"; VP-119 changelog), satisfying CLAUDE.md Canonical Principle Rule 3 (explicit human direction + concrete future dependency + named non-"later" anchor) — not a bare "follow-up documentary pass" with no anchor, which is what Finding #7 flagged as insufficient. Does not address PR #824 Finding #3 (the Edit/MultiEdit-arm `reject_canonical_symlink` guard-coverage gap itself, and its missing tests) — that is a code+test defect, routed to implementer/test-writer, out of this taxonomy-only burst's scope. No `inputs:` list change (no BC file content changed this burst — only this file's own body); input-hash unchanged. Refs: PR #824 Finding #7, BC-1.18.006. |
| 1.8 | 2026-09-08 | product-owner | Spec-text-only correction (S-25.02 cluster-2 LOCAL adversary pass-10, F-C2-P10-001, MINOR, human-authorized CONVERGE-TO-PR path): `E-SHD-001`'s Message Format cell did NOT match the actual emitted `Display` text — the shipped `ShardRollError::SealWriteFailed` (`crates/factory-dispatcher/src/shard_manager.rs`) actually emits `E-SHD-001: shard-seal-write failure for artifact_stem "{artifact_stem}" — canonical file left in its exact pre-roll state, still over cap: {source}`, not the prior `shard-seal-write failure for <artifact>: <io-error>` wording (missing the `E-SHD-001:` code prefix, the `artifact_stem "..."` quoting convention, and the "canonical file left in its exact pre-roll state, still over cap" clause). CORRECTED the cell to the exact emitted text (with `<artifact>`/`<io-error>` placeholders replacing `{artifact_stem}`/`{source}`, matching this table's existing placeholder convention). **EXHAUSTIVE E-SHD-001..009 sweep performed in this burst** (closing the recurring one-at-a-time drift pattern from pass-6/7/8/9): every `ShardRollError` variant's `#[error(...)]` Display in `shard_manager.rs` was read and diffed against this table — `E-SHD-001` (`SealWriteFailed`) was the sole drift found and is corrected above; `E-SHD-006` (`TruncateFailedAfterSeal`), `E-SHD-007` (`IndexPublishFailedAfterTruncate`), `E-SHD-008` (`BackstopProbeFailed`), and `E-SHD-009` (`SealedShardAlreadyExists`) all already match their shipped `Display` text verbatim (confirmed unchanged by pass-6/8/9's own prior corrections). `E-SHD-002` through `E-SHD-005` are NOT `ShardRollError` variants — they belong to BC-1.18.007/BC-1.18.008/BC-1.18.009/BC-1.18.011's own error surfaces (not yet implemented in `shard_manager.rs` as of this burst) and are out of this sweep's file-scope; no drift claim is made for them here. All nine `E-SHD-NNN` rows this table currently carries have now been individually source-verified at least once. **Input-hash recompute owed to state-manager** (this file's own content changed 1.7→1.8; its `inputs:` list also now reflects newer content in `BC-1.18.006.md`, concurrently amended v1.10→v1.11 in the same burst for the sibling findings F-C2-P10-002/004 — see BC-1.18.006's own changelog). |
| 1.7 | 2026-09-08 | product-owner | Spec-text-only correction (S-25.02 cluster-2 LOCAL adversary pass-9, F-C2-P9-002, MINOR): `E-SHD-008` and `E-SHD-009`'s Message Format cells did NOT match the actual emitted `Display` text — `ShardRollError::BackstopProbeFailed` (`crates/factory-dispatcher/src/shard_manager.rs`) actually emits `E-SHD-008: Write-arm backstop stat() failed for artifact_stem "{artifact_stem}" at '{path}' — cannot confirm whether the canonical file is a crash-orphaned, over-cap shard; refusing to let this Write proceed until the underlying I/O condition is resolved: {source}`, not the prior abbreviated `backstop probe stat failure for <artifact>: <io-error>` wording; `ShardRollError::SealedShardAlreadyExists` actually emits `E-SHD-009: refusing to overwrite an already-sealed shard at '{sealed_path}' for artifact_stem "{artifact_stem}" — sealed shards are write-once/immutable; this seq already has durable content on disk`, not the prior `sealed-shard immutability violation: refusing to overwrite an existing seal at <path>` wording. CORRECTED both Message Format cells to the exact emitted text (with `<artifact>`/`<path>`/`<io-error>` placeholders replacing the format-string's `{artifact_stem}`/`{path}`/`{source}` interpolations, matching this table's existing placeholder convention). **RETRACTS the v1.6 changelog's false attestation** that "all already read their actual emitted text" for rows 001-005/008/009 — that attestation was verified true only for the narrow `seq=<N>` mischaracterization it was scoped to check; it was FALSE as a general claim, since `E-SHD-008`/`E-SHD-009` themselves carried unverified, non-matching wording that this entry now corrects. BC-1.18.006 Postcondition 8's own quoted `E-SHD-009` message is reconciled to match in the same burst (BC-1.18.006 v1.9→v1.10) — see its changelog. Postcondition 7 catch point (ii)'s abbreviated `E-SHD-008` gloss ("backstop probe stat failure") and EC-019's matching CTV gloss are UNCHANGED by this entry: like the v1.6-precedent `E-SHD-006`/`E-SHD-007` mechanism prose, they describe the failure MECHANISM via a short label, never claim to be literal `Display` text, and are not in this finding's scope (F-C2-P9-002 scoped to Postcondition 8/`E-SHD-009` only) — flagged here for a future pass' consideration, not enacted. **Input-hash recompute owed to state-manager** (this file's own content changed 1.6→1.7; its `inputs:` list also now reflects newer content in `BC-1.18.006.md`, concurrently amended v1.9→v1.10 in the same burst). |
| 1.6 | 2026-09-08 | product-owner | Spec-text-only correction (S-25.02 cluster-2 LOCAL adversary pass-8, F-C2-P8-003, MINOR): `E-SHD-006` and `E-SHD-007`'s Message Format cells documented a `(seq=<N>)` token that the shipped `Display` impls (`ShardRollError::TruncateFailedAfterSeal` / `IndexPublishFailedAfterTruncate`, `crates/factory-dispatcher/src/shard_manager.rs`) never emit — the actual messages name the sealed shard by its `sealed_path` filename (e.g. `sealed shard "decision-log.0001.md"`), not a bare `seq` number. CORRECTED both Message Format cells to the exact emitted text (with `<artifact>`/`<sealed-path>`/`<io-error>` placeholders replacing the format-string's `{artifact_stem}`/`{sealed_path}`/`{source}` interpolations, matching this table's existing placeholder convention). Verified no other `E-SHD-NNN` row (001-005, 008, 009) carries a matching `seq=` mischaracterization — all already read their actual emitted text. BC-1.18.006 needs no matching change: its own Postcondition 1 text for the `E-SHD-006`/`E-SHD-007` recovery paragraphs never quoted a `seq=<N>` message string (it describes the recovery MECHANISM, not literal message text) and does not contradict the corrected text here. **Input-hash recompute owed to state-manager** (this file's own content changed 1.5→1.6; its `inputs:` list also now reflects newer content in `BC-1.18.006.md`, which was concurrently amended v1.8→v1.9 in the same burst for the sibling findings F-C2-P8-001/002/004 — see BC-1.18.006's own changelog). |
| 1.5 | 2026-09-08 | product-owner | Spec-text-only correction (S-25.02 cluster-2 LOCAL adversary pass-6, F-C2-P6-001): `E-SHD-006` and `E-SHD-007`'s "Exit Code / HookResult" column overreached and contradicted this file's own Severity Definitions table. The shipped `impl From<ShardRollError> for HookResult` (`shard_manager.rs`) maps EVERY `ShardRollError` variant — including `TruncateFailedAfterSeal` (E-SHD-006) and `IndexPublishFailedAfterTruncate` (E-SHD-007) — to `HookResult::Error`; there is no variant-specific `HookResult::Block` arm. The prior text ("`HookResult::Block` recovery / no `HookResult::Error`") wrongly implied the CRASHING dispatch itself (the roll step (c)/(d) failure) returns `Block`, which would violate Invariant 1 (no `Continue`/success-shaped result for a genuine mid-roll crash) and would falsely tell the agent the shard is now empty and safe to retry against. CORRECTED: the crashing dispatch surfaces `HookResult::Error` naming `E-SHD-006`/`E-SHD-007` (loud, matching every other E-SHD code and the Severity Definitions table's "broken ... (for E-SHD) `HookResult::Error`" row); the self-heal recovery happens SILENTLY on a SUBSEQUENT matched dispatch, BEFORE that dispatch's own trigger is evaluated (`run_self_heal_if_plausible`), and produces no distinct `HookResult` of its own on success (falls through to normal, non-Error handling) — a self-heal I/O failure surfaces its own fresh `HookResult::Error` rather than a `Block`. Code (`shard_manager.rs`'s `From<ShardRollError>` impl) was already correct; only this taxonomy's Resolution text was wrong. Verified no other `E-SHD-NNN` row (001-005, 008, 009) carries the same overreach — all already read plain `HookResult::Error` (or `HookResult::Error` / migration abort for E-SHD-005). BC-1.18.006 needs no matching change: its Postcondition 1 text for E-SHD-006/007 is silent on the crash-instant `HookResult` value (unlike E-SHD-001's explicit parenthetical) and does not contradict the corrected text. Input-hash recompute owed to state-manager (this file's own content changed 1.4→1.5). |
| 1.4 | 2026-09-08 | product-owner | Sibling-site sweep accompanying BC-1.18.006 v1.7→v1.8 (S-25.02 cluster-2 LOCAL adversary pass-4, F-C2-P4-002, MINOR): added `E-SHD-009` (sealed-shard immutability violation) — `publish_sealed_shard` is now write-once (BC-1.18.006 NEW Postcondition 8) and MUST refuse to overwrite an existing destination at a computed `seq` path, failing LOUD (`HookResult::Error`) rather than silently overwriting durable, append-only-sealed history. This is the defense-in-depth backstop for the F-C2-P4-001 data-loss finding (a fresh-context counterexample showing catch point (i) could, without a self-heal pre-pass, compute `next_seal_seq` against an unreconciled index and collide with — and silently overwrite — an already-sealed `E-SHD-006` orphan); the root-cause fix is BC-1.18.006's corrected Invariant 10/Postcondition 7 catch point (i) self-heal-first sequencing, which this code path backstops for any residual collision. See BC-1.18.006 EC-024. Input-hash recompute owed to state-manager (BC-1.18.006 input content changed v1.7→v1.8). |
| 1.3 | 2026-09-07 | product-owner | Sibling-site sweep accompanying BC-1.18.006 v1.5→v1.6 (S-25.02 cluster-2 LOCAL adversary pass-2, F-C2-P2-003, MINOR): added `E-SHD-008` (backstop probe stat failure) — Postcondition 7 catch point (ii)'s dedicated `Write`-arm `stat()` of the canonical file now fails LOUD (`HookResult::Error`) on any non-`NotFound` error, closing a previously-unadjudicated fail-open disposition that could, for a final-component symlink-loop (`ELOOP`) case, have let a `Write` apply via a non-dereferencing `write_atomic` rename after its own probing `stat()` failed. `NotFound` remains the legitimate first-ever-write case and is unaffected. Input-hash recompute owed to state-manager (BC-1.18.006 input content changed v1.5→v1.6). |
| 1.2 | 2026-09-05 | product-owner | Fix-burst amendment (adversary pass-3 finding F-P3-003, MEDIUM): corrected `E-SHD-001`'s Message Format from the stale `seal-rename failure for <artifact>: <io-error>` wording to `shard-seal-write failure for <artifact>: <io-error>` — no rename occurs under BC-1.18.006 v1.2's copy-then-atomic-truncate-in-place seal mechanism; the error code and its `HookResult::Error`/canonical-untouched contract are unchanged, only the message wording is corrected to match the actual mechanism. |
| 1.1 | 2026-09-05 | product-owner | Fix-burst amendment (adversary pass-2 finding F-P2-004, MEDIUM, ADR-051 v1.2 Decision 11): added `E-SHD-006` (shard-seal published, canonical not yet truncated — self-healing resume-from-truncate) and `E-SHD-007` (canonical truncated, index not yet updated — self-healing index reconciliation), closing the two previously-unspecified crash points in BC-1.18.006's staged per-write roll sequence. Both are self-healing (no operator intervention) rather than fail-loud aborts, distinguishing them from the existing broken/fail-loud E-SHD-001..005 codes. Required re-registering the `prd-supplement` artifact type in `plugins/vsdd-factory/config/artifact-path-registry.yaml` (found absent from the live registry despite the v1.0 changelog's claim that it was added in that burst — re-added here as a mechanical prerequisite). |
| 1.0 | 2026-09-05 | product-owner | Initial materialization of this supplement file (F-S2502-F2-006, MEDIUM). Mirrors `prd.md` §5.1's existing 7 categories (REG/PAY/CAP/PLG/SNK/ACT/HK) verbatim and adds the 8th category, `E-SHD-NNN` (Shard management errors), previously defined only in the S-25.02 F2 PRD-delta doc §4. Includes `E-SHD-001` (seal-rename failure), `E-SHD-002` (missing/corrupt shard-index), `E-SHD-003` (backfill-split content-preservation failure), `E-SHD-004` (`rotate_changelog` invocation failure), and NEW `E-SHD-005` (B2 BC-INDEX migration verification failure — content-preservation OR independent-census mismatch, BC-1.18.011). Resolves the pre-existing dangling `prd-supplements/error-taxonomy.md` reference `prd.md` §5.1/§5b have carried since before this file existed (not introduced by S-25.02; flagged by the F2 PRD-delta doc §7, closed here). Companion registry entry `prd-supplement` added to `plugins/vsdd-factory/config/artifact-path-registry.yaml` in the same burst (mechanical prerequisite — the write was blocked `ARTIFACT_PATH_UNREGISTERED` with no prior entry for this path pattern). |
